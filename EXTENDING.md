# EXTENDING.md — 택소노미 확장 가이드

> **이 가이드는 `configs/commerce_cs.json`의 대분류 A~J가 거친 절차를 체크리스트로 문서화한 것이다.**
> 원본 A~J는 전량 15,163건의 임베딩·군집화(K=12) → 사람의 원문 검토 → 2계층 재설계를 거쳤다.
> 신규 확장도 이에 준하는 검증을 거쳐야 한다.

---

## 왜 확장이 필요한가

`configs/commerce_cs.json`의 대분류 A~J는 특정 콜센터 데이터(AI Hub shopping 도메인)에서
도출되었다. 제3자의 CSV를 처리할 때 "기타" 비율이 높게 나오는 경우는 정상이며,
이는 **원본이 커버하지 못하는 업종·유형이 있다는 신호**다.

- "기타" 15% 이하 → 원본 택소노미로 충분
- "기타" 15% 초과 → PLAYBOOK의 **확장 마법사**가 자동 감지·안내

---

## 확장의 기본 원칙

1. **`configs/commerce_cs.json`은 절대 수정하지 않는다.**
   모든 확장은 `configs/local_extensions.json`에만 저장한다.
   - 원본 무결성 보존
   - `git pull` 시 충돌 방지
   - "검증된 원본 vs 내 확장" 이력 명확화

2. **아래 체크리스트 6개 필드 중 하나라도 비면 정식 카테고리로 인정하지 않는다.**
   부분 작성 상태는 `local_extensions.json`의 `status: "draft"`로만 저장한다.

3. **AI가 전체 필드를 한꺼번에 채워서 제안하지 않는다.**
   PLAYBOOK 확장 마법사는 필드를 한 번에 하나씩, 매 필드 사용자 확인 후 진행한다.

---

## 확장 체크리스트 (6개 필드 전부 필수)

신규 대분류를 추가하려면 아래 6개 필드를 모두 채워야 한다.

```
[ ] 1. 정의 (한 문장)
       - 이 카테고리가 다루는 것이 무엇인지 한 문장으로 서술
       - 예: "구매한 상품이 정품이 아닐 수 있다는 의심·확인 요청"

[ ] 2. 포함 예시 (자신의 데이터에서 원문 인용 최소 2건)
       - 반드시 처리 중인 CSV에서 voc_id와 함께 원문을 직접 인용
       - 각 예시에 source 필드를 명시할 것:
         · source="real"    처리 중인 CSV의 실제 voc_id 원문. 근거 건수에 산입됨
         · source="임의 예시"  정의 이해를 돕는 보조 예문. 근거 건수에 산입하지 않음
       - confirmed 저장 조건: source="real" 이 2건 이상 포함되어야 함
         source="real"이 2건 미만이면 6개 필드를 모두 채워도 status는 "draft"로 저장
       - 예시:
         {"source":"real", "voc_id":"42", "text":"이 제품 혹시 짝퉁 아닌가요? 로고가 이상해요"}
         {"source":"real", "voc_id":"87", "text":"정품 인증서 받을 수 있나요"}
         {"source":"임의 예시", "voc_id":null, "text":"(보조 이해용 — 근거 건수 미산입)"}

[ ] 3. 제외 항목 (기존 카테고리와의 경계 + 행선지 명시)
       - 이 카테고리와 헷갈릴 수 있는 기존 카테고리를 명시하고
         어떤 경우는 기존 카테고리로 보내야 하는지 경계를 그어야 한다
       - 행선지(route_to)가 없는 제외 항목은 인정하지 않는다
       - 예:
         "제품 하자·불량" → C1 (외관 문제는 정품 의심이 아니라 불량)
         "사용법 문의" → J1 (사용 방법을 모르는 것은 정품/가품과 무관)

[ ] 4. 귀책태그 매핑 (기존 6개 중 선택, 신설 시 사유 필수)
       기존 태그: [판매자] [택배사] [플랫폼/시스템] [고객] [정보/매뉴얼] [?불명]
       - 이 카테고리의 주요 원인에 해당하는 태그를 선택
       - 기존 6개로 커버되지 않는 경우에만 신규 태그를 제안하고
         신설 사유를 반드시 명시

[ ] 5. 대응 Action 최소 1개
       - context/action_playbook.md의 Action 코드 체계를 따를 것
       - 기존 Action 카탈로그에서 재사용 가능한 것이 있으면 그것을 우선 사용
       - 신규 Action이 필요하면 코드(예: K-a1), 이름, 원인, 유형, 지표를 기재

[ ] 6. 연결 Metric
       - context/metric_dictionary.md의 1차 지표 7개 중 하나 이상 선택
       - 이 카테고리의 개선을 어떤 지표로 추적할 것인지 명시
```

---

## 확장 등록 절차 (PLAYBOOK 확장 마법사 요약)

1. **06단계(Topic 분류) 완료 후**, "기타" 비율이 15%를 초과하면 AI가 자동 감지
2. AI가 "기타"로 분류된 VOC 중 대표 사례를 **원문+voc_id** 형태로 제시
3. 사용자가 확장을 원하면, **필드를 한 번에 하나씩** 대화형으로 진행
   - 매 필드마다 사용자 확인 필수
   - AI가 전체를 한꺼번에 채워서 제안하는 것은 금지
4. 6개 필드 모두 완료 → confirmed 저장 여부 판정:
   - include_examples 중 source="real" 이 2건 이상 → `status: "confirmed"` 저장
   - source="real" 이 2건 미만 → `status: "draft"` 저장 + 사용자에게 사유 고지:
     "실발화 근거 N건으로 confirmed 저장 불가. draft로 저장하며 분류에는 사용됩니다.
      실제 CSV에서 근거 2건 이상 확보 시 confirmed으로 승격할 수 있습니다."
5. 07단계(Sentiment 분류) 진행 시 확장된 카테고리 포함 (draft 포함 모두 사용)

---

## 예시 — 완성된 확장 항목

아래는 확장 마법사를 통해 완성된 카테고리 예시다.
(실제 데이터에서 voc_id와 원문을 인용해야 함 — 아래는 형식 예시)

```json
{
  "id": "K",
  "name": "정품·진위 확인",
  "status": "confirmed",
  "definition": "구매한 상품이 정품인지 확인하거나 가품 의심을 신고하는 문의",
  "include_examples": [
    {"voc_id": "42", "text": "이 제품 혹시 짝퉁 아닌가요? 로고가 이상해요"},
    {"voc_id": "87", "text": "정품 인증서 받을 수 있나요"}
  ],
  "exclude": [
    {"pattern": "외관 불량·파손", "route_to": "C1"},
    {"pattern": "사용법 문의", "route_to": "J1"}
  ],
  "cause_tags": ["[판매자]", "[?불명]"],
  "actions": [
    {
      "id": "K-a1",
      "name": "정품 인증 페이지·QR코드 제공",
      "cause": ["K-c1"],
      "type": "[콘텐츠/FAQ]",
      "metric": "토픽 VOC 비중"
    }
  ],
  "metrics": ["토픽 VOC 비중", "부정 감정 비율"]
}
```

---

## 참고 — 미검증 관찰 기록

이전 분석 세션에서 관찰된 후보 카테고리는 `candidate_extensions.md`에 보존되어 있다.
확장 마법사 진행 시 참고 사례로 활용할 수 있으나, 검증을 거치지 않은 관찰 기록이므로
`configs/commerce_cs.json`에 반영되지 않았다.
