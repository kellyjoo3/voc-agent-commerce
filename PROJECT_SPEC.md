# VOC Insight Agent — PROJECT_SPEC

VOC 기반 Decision Support MVP의 **확정 사양**. 구현은 이 문서를 단일 기준으로 따른다.

> **구현 원칙**
> 이 문서의 수식·임계값·경계는 사람이 결정한 것이다. 구현 주체(AI 포함)는 임의로 변경하지 않는다.
> 변경이 필요하다고 판단되면 구현을 멈추고 근거와 함께 제안한다.

| 항목 | 값 |
|---|---|
| 문서 버전 | v1.8 |
| 최종 수정 | 2026-08-27 |
| 상태 | **완전 확정.** 택소노미 2계층 + Domain Context 4종 + 500건 본 분류(대/중분류+Sentiment) 완료 |
| 관련 문서 | [DATA_PROFILE.md](DATA_PROFILE.md) · [production_architecture.md](production_architecture.md) · [topic_taxonomy.md](context/topic_taxonomy.md) · [의사결정기록 #01](docs/의사결정기록_01_토픽개수_K선정.txt) |

> **v1.8 변경:** production_architecture.md §4에 SKT 혼합 검증 메커니즘 구체화 — "실패 추정" 판별 신호(중분류 null/대분류 기타/2차 자기점검 불일치)를 우리 택소노미 설계에 매핑, 절차 전환 전후 비교표 추가. 500건 Human 라벨링을 "04 실행"이 아닌 "최초 캘리브레이션 표본"으로 재정의.
> **v1.7 변경:** 500건 본 분류 완료 — 대분류 최종 확정(도메인 필터 반영) + Sentiment 4단계. 대분류별 평균 심각도가 뚜렷이 갈림을 실증(C 2.54 ~ J 0.95), §6.2 변별력 우려 해소. production_architecture.md에 "표본 검증→확대" 절차가 현업에서도 반복 루틴으로 존속함을 업계 사례(SKT 에이닷) 근거로 반영.
> **v1.6 변경:** 500건 파이프라인 수기 검증(§12)에서 이커머스 외 업종 혼입 발견 → 도메인 적합성 사전 필터 신설(§3), 전량 156건(1.03%) 격리(삭제 아님). production_architecture.md에 MVP 한정 이슈로 기록.
> **v1.5 변경:** §15 신설 — 현업 실제 활용 시 변경되는 부분. 운영 전환 설계를 [production_architecture.md](production_architecture.md)로 분리하고 주요 변경 지점·재사용성·기술 스택 입장을 요약.
> **v1.4 변경:** Topic 택소노미 K=12 확정 및 실행 결과 반영(§5) / Label Guideline(정의·포함·제외+행선지) 규정(§5) / 검증을 Human 100건 라벨링 기반 accuracy·precision·recall·confusion matrix로 격상, 층화 추출·약한 토픽 식별 명시(§12) / topic_taxonomy.md를 문서 목록에 추가(§8).
> **v1.3 변경:** Business Impact를 공식에 넣지 않는 근거 명문화(§1-A, 부서 경계 논리) / metric_dictionary 1·2차 계층 구조(§8) / 현업 대응 문서 매핑표(§8) / Expected Effect 정량화(§9) / 대시보드 기대효과 표시(§14) / 표본 확장성 및 분류 캐시·재개(§3, §13).
> **v1.2 변경:** Priority 등급 컷오프 확정(§6.4) — `P1 = 상위 3개 AND ≥50`. 역량선(N=3) + 하한선(공식 중심점 50) 결합 기준.
> **v1.1 변경:** 데이터 실측 프로파일링 반영. 모집단 확정(§2), 인코딩·PII(§3), 날짜 사용 불가 근거 확보(§4), Sentiment 프롬프트 규칙 추가(§6.2).

---

## 1. 목적과 범위

### 목적
VOC를 투입하면 **어떤 문제를 왜 먼저 해결해야 하는가**를 산출하는, 실제로 동작하는 최소 Decision Support Demo.

### 성공 기준
500건의 VOC를 사람이 개별적으로 읽지 않고, 반복 이슈와 주요 원인 및 우선순위를 **60초 이내**에 파악한다.

> 원 설계의 "30초"에서 조정. 500건 LLM 분류를 배치(25건/호출) + 병렬(8~10)로 처리해도 40~60초가 현실적이다. 달성 불가능한 수치를 내걸지 않는다. 실측값을 README에 기재한다.

### 범위에 포함
VOC 적재 → 정제 → Topic 분류 → Sentiment 분류 → Frequency/Severity/Trend 산출 → Priority 랭킹 → Root Cause 가설 → Recommendation → 1페이지 대시보드

### 범위에서 제외 (명시적)
- 모델 파인튜닝 (LoRA/QLoRA, KLUE-RoBERTa 학습 등) — 프롬프트 기반 LLM만 사용
- 벡터DB / RAG 인프라 — 근거 인용은 인메모리 조회로 처리
- Priority 공식에 Business Impact 항 추가, Actionability 지표 — §1-A 참조
- Critical Issue Override Rule — 필요성은 인정하나 범위 확대 방지를 위해 보류 (§10 참조)
- 이탈 예측, CRM 연동, 실시간 파이프라인, 인증/멀티테넌시

### 1-A. Business Impact를 공식에 넣지 않는 이유 — 부서 경계

원 설계 초안에는 Priority 요소로 Business Impact(매출·이탈·CS비용 영향)가 있었다. 이를 3항 공식(F/S/T)에 4번째 항으로 추가하지 않기로 확정한다.

**데이터 근거가 아니라 역할 근거다.** 단순히 "데이터가 없어서"가 아니라, VOC 분석 조직과 사업 영향을 판단하는 조직이 원래 다르기 때문이다. 참고자료(오픈서베이 VoC 분석 사례)의 지적대로 *"VoC를 수집하는 부서와 개선을 담당하는 부서가 다르다"* — CS/VOC 분석은 "무엇이·얼마나·왜"를 정리해 넘기고, 사업 임팩트 환산은 넘겨받은 부서가 자신의 매출·비용 데이터와 결합해 수행한다.

```
[CS · VOC 분석 — 본 프로젝트의 범위]
   무엇이 · 얼마나 자주 · 얼마나 심각하게 · 늘고 있는가
   → 우선순위 후보 + 원인 가설 + 실행안 + 기대효과(§9)
              │
              ↓ 유관부서 전달
[사업 · 제품 부서 — 범위 밖]
   그래서 매출 · 이탈 · 비용에 얼마인가
   → 최종 투자 결정
```

Business Impact를 넣지 않은 것은 **VOC 분석의 책임 범위를 정확히 그은 것**이며, 데이터가 없어서 어쩔 수 없이 뺀 것과는 다르다. 3항을 유지하면 근거 없는 4번째 가중치를 설명해야 하는 부담도 함께 피한다.

**완전히 버리지는 않는다.** 아래 세 곳에 형태를 바꿔 담는다.
1. §8 `metric_dictionary.md`를 1차(VOC 관측)/2차(사업) 지표로 분리해 **연결 지점**을 남긴다
2. §9 Recommendation의 Expected Effect를 Frequency 기반으로 **정량화**해, 유관부서가 비용으로 즉시 환산할 수 있게 한다
3. README에 확장 설계로 명시한다: *"사업 데이터가 연결되면 `Priority = 0.3F + 0.3S + 0.2T + 0.2B`로 확장 가능하다. 본 데모는 공개 데이터의 관측 범위 안에서 3항으로 한정했다."*

---

## 2. 데이터셋

### 확정
**AI Hub「용도별 목적대화 데이터」(dataSetSn=544)**

**확보 완료. 아래는 실측값이다** (상세: [DATA_PROFILE.md](DATA_PROFILE.md)).

| 항목 | 내용 |
|---|---|
| **모집단** | **shopping 도메인 15,329건** (Training 13,839 + Validation 1,490) |
| 사용 파일 | `TL_1.shopping.zip`, `VL_1.shopping.zip` (라벨링데이터만) |
| 미사용 | 원천데이터(`TS_`/`VS_`), civil complaint / education / tourism 도메인 |
| 형식 | 대화 1건 = JSON 1파일. **인코딩 CP949** |
| 채널 | 콜센터 84.4% / SNS 게시판 15.6% |
| 카테고리 | 제품사용문의 2,773 / AS문의 2,726 / 온오프라인안내 2,336 / 주문결제 2,152 / 배송 2,142 / 환불반품교환 2,110 / 이벤트 1,090 |
| 기존 라벨 | 주제(7종), 화행(speechAct), 화자 구분(A=상담원 / B=고객) |

> **도메인 선별:** 내려받은 데이터는 4개 도메인으로 구성된다. `shopping`만 사용한다. `civil complaint`는 공공 민원이라 사기업 VOC와 발화 목적·어휘가 다르고, `education`/`tourism`은 도메인이 불일치한다.
>
> **Training/Validation 통합 사용:** 본 프로젝트는 모델 학습이 아니라 분석이므로 분리가 불필요하다.

**선정 근거**

1. 규모가 1-2일 일정에 적정 (55만 쌍 데이터셋은 파싱·다운로드 자체가 리스크)
2. 주제 구성이 본 설계의 예시 시나리오(배송 지연/환불 지연/결제 오류)와 일치 → 도메인 컨텍스트를 억지 없이 작성 가능
3. **기존 주제 라벨 존재** → Topic 분류 정확도를 정량 검증 가능 (§12)
4. **화행 라벨 존재** → Severity 분류의 교차검증에 활용 ("지시하기"는 강한 요구 → Severe Negative와 양의 상관이 기대됨)

### 백업
**네이버 쇼핑 리뷰** (bab2min/corpus, 20만 건 TSV, 인증 불필요)

AI Hub 인증 또는 CLI가 막힐 경우 즉시 대체한다.
**요구사항: 데이터 로더를 어댑터로 분리하여, 백업 전환 시 로더 교체만으로 파이프라인 전체가 동작해야 한다.** 데이터 확보가 전체 일정을 인질로 잡지 않게 한다.

### 보조 (도메인 컨텍스트 근거자료 전용, 분석 대상 아님)
- 한국소비자원 품목별 피해구제 사례 (data.go.kr 15090382, XML 678건)
- 공정거래위원회 1372 소비자상담 통계 API (data.go.kr 15127091)

> 이 두 자료는 §8의 Cause Taxonomy / Action Playbook을 **상상이 아닌 공개 자료에 근거**해 작성하기 위한 유일한 수단이다. 규모가 작아 분석 대상으로는 쓰지 않는다.
>
> 참고: 1372 API는 접수일자·품목코드·성별·연령 등의 **집계 통계이며 상담 원문 텍스트가 없다.** 분석 데이터로 오인하지 말 것.

---

## 3. 데이터 스키마

### 분석 단위
**VOC 1건 = 대화 1건의 고객 발화(turn)를 시간순으로 결합한 텍스트.** 상담원 발화는 제외한다.

### 최소 스키마

| 필드 | 필수 | 출처 | 용도 |
|---|---|---|---|
| voc_id | 필수 | 생성 | 식별, 중복 제거, 근거 인용 |
| created_at | 필수 | **합성 (§4)** | Trend 계산 |
| text | 필수 | 원본 | Topic / Sentiment / Root Cause |
| topic | — | **AI 생성** | Frequency, Severity, Trend |
| sentiment | — | **AI 생성** | Severity |
| src_topic | — | 원본 라벨 | **검증 전용. 파이프라인에 투입 금지** |
| src_speech_act | — | 원본 라벨 | **검증 전용. 파이프라인에 투입 금지** |

> src_* 는 정답지다. 분석 파이프라인이 이 값을 참조하면 검증이 무의미해진다. 로딩 후 즉시 분리 보관한다.

### 샘플링
전체에서 **500건**을 원본 주제 분포에 따라 층화 추출한다. 시드 고정.

> 목표 건수는 500으로 고정한다. 원 자료의 1,000건·3,284건 표기는 폐기한다.

**500건인 이유는 비용이 아니라 반복 개발 속도다.** 개발 중 프롬프트·로직을 5~10회 재실행하게 되는데, 500건은 1회 약 1분, 15,329건 전량은 1회 약 10분이다. 전량 기준으로는 반복 루프가 100분대가 되어 개발 자체가 막힌다.

**모집단 규모(15,329건)로 확장 가능한 구조다.** 표본 크기 N은 코드 상수 파라미터로 둔다. N이 커져도 아키텍처는 그대로다 — 늘어나는 것은 LLM 분류 호출 수뿐이며(배치+병렬 구조 그대로 선형 증가), 택소노미 발견(§5, 샘플 500건이면 충분), 지표·Priority 계산(pandas 집계, 1초 미만), Root Cause·Recommendation(토픽 단위, 원본 건수와 무관)은 N과 무관하다. 최종 데모는 전량(15,329건)으로 1회 실행해 확장성을 실증할 수 있다.

**Trend 표본 부족 시 대응:** §6.3의 표본 부족 임계(토픽×기간당 10건 미만)에 절반 이상의 토픽이 걸리면 N을 1,500건 등으로 상향한다. 코드는 그대로고 상수만 바뀐다.

### 정제 기준

| 대상 | 처리 |
|---|---|
| voc_id | 중복 제거 |
| created_at | 형식 통일, 결측 확인 |
| text | 빈 텍스트 제거 / 중복 VOC 제거 / 불필요한 공백·줄바꿈 정리 |
| text | 지나치게 짧아 분석 불가능한 건 확인 후 제외 (임계값은 프로파일링 시 기록) |
| PII | **이미 가명처리되어 있음** (`#@이름#` `#@전번#` `#@주소#` 등). 잔존 패턴 검사만 수행 |

**인코딩:** 원본이 CP949다. 로더는 `cp949` → `euc-kr` → `utf-8` 순으로 폴백한다.

**PII 처리 (축소 확정):**
원문에 PII가 이미 `#@이름#`(20,462회) `#@전번#`(4,117회) 등의 토큰으로 치환되어 있다. 별도 마스킹 모듈 대신 다음만 수행한다.
1. 전화번호·계좌번호 정규식으로 **잔존 검사**. 검출 건수를 기록하고, 0이면 마스킹 미적용
2. 토큰 매칭은 `#@[^#]+#` 로 한다. `#@소속 #` 같은 **공백 포함 변형이 실재**하므로 고정 문자열 매칭은 실패한다

**과도 정제 금지.** 느낌표 반복, 반복 표현, 부정 표현은 감정 신호이므로 보존한다. 특수문자 일괄 삭제는 금지한다. 가명처리 토큰(`#@...#`)도 그대로 유지한다 — 제거하면 문장이 끊긴다.

### 도메인 적합성 사전 필터 (추가 — 500건 검증에서 발견)

**배경:** §12 500건 파이프라인 검증(건별 수기 분류) 중, 원본 데이터에 **이커머스 외 업종**(여행·게임·렌탈·통신·행정·B2B·오프라인 예약 등)이 소수 혼입되어 있음을 발견했다. `DATA_PROFILE.md`의 집계 통계(분포·PII·길이)로는 이 혼입이 잡히지 않았다 — 건별로 읽어야 보이는 문제였다.

**처리 (2단계, 삭제 없이 격리):**

```
① 키워드 사전 필터 (Code, app/domain_filter.py)
   명백한 업종외 패턴만 격리 → data/voc_all.csv 의 domain_flag 컬럼에 태그
   전량 15,163건 중 156건(1.03%) 격리 (게임 65 / 여행숙박항공 55 /
   렌탈구독대여 14 / 통신개통 8 / 오프라인예약 5 / 행정공공 4 /
   디지털콘텐츠 3 / B2B판매자 2)
      ↓
② 04 Topic 분류 (LLM)
   ①을 통과한 애매한 잔여 건(키워드로 못 잡는 표현)은
   LLM이 판단해 `기타`로 자연 처리
```

`domain_flag`가 있는 행은 §5 Topic 분류·§6 Priority 계산에서 **제외**한다(삭제가 아니라 제외). 이유:
- Frequency·Trend는 "이커머스 이슈가 늘었다"는 전제로 계산되는데, 타 업종 트래픽이 섞이면 그 전제가 깨진다
- Cause Taxonomy·Process Map은 이커머스 주문 흐름(결제완료→배송완료)을 전제하므로 타 업종에는 적용되지 않는다

**① 키워드 필터의 한계 (정직하게 기록):** 500건 수기 검증에서는 업종외 비율이 8.6%로 관측됐으나, 이는 대분류 균등 층화 표본(작은 클러스터 과대표집)의 결과이지 모집단 비율이 아니다. 키워드 필터가 잡는 건 명백한 패턴뿐이며, "맥락 불명확"류(피싱 의심, 통화품질 문제 등)는 못 잡는다. **최종 `기타` 비율은 ②(LLM 분류) 이후 확정된다.**

> **production_architecture.md 참고:** 실제 현업에서는 채널이 업종별로 이미 분리되어 있어 이런 혼입이 원천적으로 발생하지 않는다. 이 필터는 **공개 데이터 특유의 보정**이며, MVP 한정 조치다.

---

## 4. 합성 타임스탬프 (Trend 전제)

### 배경 — 실측으로 확정됨

데이터에는 날짜로 보이는 필드가 **3종류** 있으나 **모두 VOC 발생 시점이 아니다.**

| 출처 | 고유값 | 판정 |
|---|--:|---|
| `info[0].date` | **3개** | 구축(납품) 일자. 2021-11-01 하루에 10,162건 집중 |
| `dataset.name` 내 8자리 | 26개 | 가공 세션 일자 |
| `info[0].filename` 내 8자리 | 38개 | 수집 세션 일자 |

**결정적 근거:** 카테고리별 수집 배치가 분리되어 있다. `07.온오프라인안내`는 **100%가 2021-11에만** 존재하고, `06.이벤트`는 98%가 2021-11이다. 이 날짜로 Trend를 계산하면 "온오프라인안내가 11월에 무한대 급증"이라는 결과가 나오는데, 이는 고객 문의의 변화가 아니라 **데이터 수집 일정의 인공물**이다.

> 이런 값은 순수 합성보다 오히려 위험하다. 실측처럼 보이지만 실제로는 수집 스케줄을 측정하기 때문이다.

따라서 created_at은 **합성한다.**

### 규칙

1. 분석 기간을 2개 구간으로 설정한다: **직전 기간 / 최근 기간** (각 동일 길이)
2. 시드를 고정한다. 동일 입력에 동일 결과가 나와야 한다
3. 랜덤 균등 배정이 아니라 **토픽별로 의도한 추세를 심는다.** 일부 토픽은 증가, 일부는 감소, 나머지는 보합
4. 심은 추세의 정답값을 `data/trend_ground_truth.json`에 기록한다

### 검증 용도
심은 추세와 파이프라인이 산출한 Trend Score를 대조하여 **Trend 계산 로직 자체의 정확성**을 검증한다. 이것이 합성을 랜덤이 아니라 설계된 방식으로 하는 이유다.

### 고지 의무 (필수)
다음 세 곳에 **모두** 명시한다. 누락 시 결과가 실측으로 오인된다.

- 데이터 파일 및 `data/README.md`
- 대시보드 Trend 컬럼 인근
- README의 한계 섹션

> 고지 문구: **"created_at은 Trend 로직 검증을 위해 임의 부여한 값이며 실제 발생 시점이 아니다. Trend 수치는 실측이 아니다."**

---

## 5. Topic 확정 절차

### 문제
설계는 Topic을 비지도 클러스터링으로 사후 생성한다. 그런데 (a) Cause Taxonomy는 Topic이 사전에 확정되어야 작성 가능하고, (b) 클러스터 경계가 실행마다 달라지면 기간 간 Topic 정의가 흔들려 Trend 계산이 무의미해진다.

### 해결 — 2단계 분리

**1단계: 택소노미 발견 (오프라인 1회) — 실행 완료**

```
전량 15,163건 → Embedding → Clustering(K=12) → 클러스터별 대표 VOC 확인
              → 사람이 원문 검토로 확정 → context/topic_taxonomy.md
```

- 임베딩: jhgan/ko-sroberta-multitask (전량 38분 소요, `output/embeddings_full.npy` 캐시)
- 폴백: 설치 실패 시 TF-IDF + KMeans로 클러스터 초안 → LLM 라벨링
- **산출된 택소노미는 §8 도메인 문서 4종의 근거가 된다** (상상이 아님)

**확정 결과 (근거: [의사결정기록 #01](docs/의사결정기록_01_토픽개수_K선정.txt))**
- **K=12로 확정.** 12→14 확장 시 새 토픽 없이 노이즈만 증가하여 12가 의미적 상한임을 확인
- 12개 클러스터 중 **노이즈 2개(신원확인 PII 패턴, 통화 서두 템플릿 패턴)를 `기타`로 흡수** → **실질 토픽 10개**
- 노이즈는 내용이 아니라 말투·문장구조로 묶인 껍데기다. 사람이 원문 20건씩 검토해 식별했다
- 표본 규모: 당초 500건 dev용에서 전량 15,163건으로 확장(§3) — 세분화 토픽의 Trend 표본 부족 방지

**Label Guideline (필수) — 토픽 경계를 고정한다**

`topic_taxonomy.md`의 각 토픽은 다음 3요소를 반드시 갖는다.

| 요소 | 내용 |
|---|---|
| 정의 | 이 토픽이 무엇인가 (한 줄) |
| 포함 | 이 토픽에 들어가는 발화 유형 + 예시 |
| 제외 | 헷갈리지만 **다른 토픽으로 보낼** 유형 + **행선지 명시** |

예:
```
[배송 지연/조회]
  정의: 주문한 상품의 배송 진행·도착에 관한 문의·불만
  포함: 배송 늦음, 출고 지연, "언제 오나요", 배송 상태 조회
  제외: 택배 파손   → [반품/환불/교환]
        오배송      → [반품/환불/교환]
        배송지 변경  → [주문/결제 변경]
```

> **제외 항목에 행선지가 없으면 안 된다.** 없으면 LLM이 실행마다 경계 케이스를 다른 곳에 넣어 토픽이 흔들리고, §6.3 Trend 계산의 전제(토픽 정의 불변)가 깨진다. Guideline의 존재 이유가 이것이다.

**2단계: 런타임 분류**

```
VOC → 고정 택소노미 + Label Guideline 기반 LLM 분류 → topic
```

- Guideline을 분류 프롬프트에 그대로 주입한다 → 실행마다 동일한 Topic 집합 → Trend 안정
- 어느 Topic에도 속하지 않으면 `기타`로 분류한다. `기타` 비율을 지표로 기록한다

> 클러스터링은 "택소노미 발견용", 런타임은 "분류"다. 이 분리가 순환 의존을 끊는다.

---

## 6. Scoring — 확정 사양

> **모든 지표는 0~100으로 표준화한다.** 이것이 이 절의 핵심이다.

### 6.1 Frequency

**Frequency (원본 지표)**

```
Frequency(%) = 해당 Topic VOC 수 / 전체 VOC 수 × 100
```

대시보드에는 이 원본 비중(%)을 표시한다.

**F_score (Priority 계산용)**

```
F_score = min(Frequency(%) / 20, 1) × 100
```

#### 왜 원본 비중을 그대로 쓰지 않는가

`Priority = 0.4F + 0.4S + 0.2T`는 발생량과 심각도를 반반 보겠다는 의도다. 그런데 두 지표의 실질 범위가 다르다.

| 지표 | 실질 범위 | 이유 |
|---|---|---|
| Severity | 0 ~ 100 | 온전한 100점 만점 |
| Frequency(%) | 0 ~ 25 내외 | 전체 대비 비중이므로 Topic 개수가 상한을 누른다. Topic 10개면 평균 10% |

같은 0.4를 곱해도 벌릴 수 있는 점수 폭이 4배 차이난다. **표기는 0.4 : 0.4 지만 실제 작동은 대략 0.15 : 0.4 이며, Priority 랭킹이 사실상 Severity 랭킹이 된다.**

구체적으로:

| Issue | 건수 | Frequency(%) | S | T |
|---|--:|--:|--:|--:|
| 배송 지연 | 100 | 20 | 45 | 50 |
| 환불 지연 | 50 | 10 | 75 | 50 |

원본 비중 사용 시:

```
배송 지연 = 0.4(20) + 0.4(45) + 0.2(50) = 36
환불 지연 = 0.4(10) + 0.4(75) + 0.2(50) = 44   ← 건수 절반인데 이김
```

F_score 사용 시:

```
배송 지연 = 0.4(100) + 0.4(45) + 0.2(50) = 68   ← 건수 2배가 순위에 반영됨
환불 지연 = 0.4( 50) + 0.4(75) + 0.2(50) = 60
```

#### 20% 기준선의 근거

**20%는 데이터에서 도출한 값이 아니라 사전에 선언한 의사결정 기준이다.**

> **"전체 VOC의 20% 이상을 차지하는 단일 이슈는 조직의 주요 반복 이슈로 간주한다."**

이 기준선은 데이터를 보기 전에 정의되었으며, **데이터 분포에 맞춰 사후 조정하지 않는다.** 그래야 다른 데이터를 투입해도 동일한 잣대로 측정한다고 말할 수 있다.

README 기재 문구: *본 프로젝트에서는 주요 이슈의 판단 기준을 전체 VOC의 20%로 설정하였다.*

#### 채택하지 않은 대안 — 상대 정규화

```
F_score = 해당 Topic VOC / 최다 Topic VOC × 100     ← 미채택
```

기준선이 데이터에 따라 움직인다. 어제는 20%가 100점, 오늘은 10%가 100점이 되어 **절대적 규모를 반영하지 못한다.** 기간을 나눠 계산하는 구조에서 특히 불안정하다.

#### 알려진 동작

상한 100 때문에 20% 이상인 Topic이 둘 이상이면 F_score가 동점이 된다. 이는 "20% 이상은 모두 주요 반복 이슈"라는 기준선의 의도된 결과다. 동점 발생 시 Severity로 순위가 갈린다.

#### 20%는 상한(cap)이다 — 동작 방식

`min(..., 1)`이 상한 역할을 한다. **20%를 "만점선"으로 두고, 그 이상은 모두 100점으로 처리한다.**
시험에서 20문제를 맞으면 100점인데 25문제·40문제를 맞아도 똑같이 100점인 것과 같다.

| Topic 비중 | 계산 | F_score |
|---|---|--:|
| 5% | 5÷20 = 0.25 | 25 |
| 10% | 10÷20 = 0.50 | 50 |
| 15% | 15÷20 = 0.75 | 75 |
| 20% | 20÷20 = 1.00 | 100 |
| 25% | 1.25 → **1로 깎임** | **100** |
| 40% | 2.00 → **1로 깎임** | **100** |

20%까지는 비례해 상승하고, 그 이후로는 평평하다.

**상한을 두는 이유:** 없으면 거대 Topic 하나가 순위를 독식한다. 40% Topic이면 F_score 200,
가중치 0.4를 곱해 80점이 되어 **S(감정)와 T(추세)가 무의미해진다.**
상한의 메시지는 *"20%를 넘으면 모두 '매우 빈번함'이며, 그 이상은 구분하지 않는다. 나머지는 감정·추세로 판단한다"* 이다.

#### 실측 사례 — I 대분류 (3,000건)

| 시점 | 비중 | F_score |
|---|--:|--:|
| **분할 전** I(제품 A/S + 사용법), 전량 기준 | 25.8% | **100 (상한에 깎임)** |
| **분할 후** I(제품 A/S), 3,000건 실측 | **19.80%** | **99.0** |
| 분할 후 J(제품 사용법), 3,000건 실측 | 3.54% | 17.7 |

분할 전에는 25.8%든 30%든 **동일하게 100점**이라 실제 규모가 점수에 나타나지 않았다.
`topic_taxonomy.md` v1.1에서 I/J를 분할한 뒤에야 19.80 → 99.0으로 **규모가 점수에 반영**됐다.

> **I의 99.0은 "측정 가능한 범위의 거의 끝"을 뜻한다.** 조금만 더 컸으면 상한에 걸려
> 얼마나 큰지조차 드러나지 않았을 값이다.

#### 주의 — 상한 구간에서의 점수 증폭

20% 구간을 100점으로 늘려 쓰므로 **비중 차이가 점수 차이로 증폭된다.**

```
I 19.80%  vs  D 18.12%   →  실제 규모 차이 1.68%p
F_score 99.0  vs  90.6   →  점수 차이 8.4점  (약 5배 증폭)
```

순위표를 읽을 때 **점수 격차를 규모 격차로 곧바로 해석하면 과대평가**가 된다.
대시보드에는 F_score와 함께 **원본 비중(%)을 병기**한다(본 절 상단 규정).

### 6.2 Severity

**Sentiment 라벨 (VOC 단위, AI 분류)**

| 라벨 | 기준 | 예시 | 점수 |
|---|---|---|--:|
| Positive | 만족·감사 | "빠른 처리 감사합니다" | 0 |
| Neutral | 단순 문의·정보 요청 | "배송 언제 오나요?" | 1 |
| Negative | 불편·지연·오류 | "예정일보다 늦어요" | 3 |
| Severe Negative | 강한 불만·환불 요구·이탈 의사 | "최악입니다. 환불해주세요." | 5 |

**S_score (Topic 단위)**

```
Severity_raw = Σ(해당 Topic VOC의 감정점수) / 해당 Topic VOC 수     # 0~5
S_score      = Severity_raw / 5 × 100                              # 0~100
```

**분류 프롬프트 필수 규칙 (실측 반영)**

거의 모든 대화가 "감사합니다 / 고맙습니다 / 수고하세요"로 끝난다. 긍정 키워드 출현율이 카테고리별 36~71%에 달하는 것은 대부분 이 종료 인사 때문이다.

> **프롬프트에 명시:** *"통화 시작·종료 인사말(안녕하세요, 감사합니다, 수고하세요 등)은 감정 판단에서 제외한다. 문의·불만의 본문만으로 판단한다."*

누락 시 다수가 Positive로 오분류된다.

**변별력 확인 (필수)**

사전 점검 결과, 부정 키워드 포함률이 카테고리별로 **11.9%(제품사용문의) ~ 76.8%(환불반품교환)로 6.5배 차이**난다. Topic 간 Severity 변별은 될 가능성이 높다.

다만 **Severe Negative가 희소하다.** 콜센터 녹취라 고객이 예의를 지키며, 화행 라벨 `감정표현하기(불평하기)`가 전체 발화 중 575회에 불과하다. **S_score의 상단(80~100)이 나오지 않고 전체가 20~60 범위에 분포할 가능성이 높다.**

→ LLM 분류 실행 후 Topic별 S_score 분포를 확인하고 기록한다. 좁은 구간에 수렴하면 Severe Negative 비율을 별도 지표로 대시보드에 병기한다. **점수 배점(0/1/3/5) 자체는 변경하지 않는다.**

### 6.3 Trend

**Trend_raw (Topic 단위)**

```
Trend_raw(%) = (최근기간 비중 - 직전기간 비중) / 직전기간 비중 × 100
```

건수 증가율이 아니라 **전체 VOC 대비 비중의 변화율**을 사용한다.

**T_score — 선형보간**

| Trend_raw | T_score |
|--:|--:|
| -50% 이하 | 0 |
| -25% | 25 |
| 0% | 50 |
| +25% | 75 |
| +50% 이상 | 100 |

구간 사이는 선형보간한다. (예: +20% → 50 + 20/25 × 25 = 70)

**표본 부족 시 중립 처리 (필수)**

```
if (직전기간 Topic VOC < 10) or (최근기간 Topic VOC < 10):
    T_score = 50    # 중립
```

> 500건을 2구간으로 나누면 구간당 250건, Topic 10개면 Topic당 약 25건이다. 1~2건 차이로 비중 변화율이 ±20% 흔들린다. 임계 미만은 판단을 보류한다. 대시보드에 "표본 부족" 표기.

### 6.4 Priority

> ## Priority는 실행 순서가 아니다
>
> **Priority는 F·S·T 세 항만을 재는 공식에 기반한 순위이며, Code가 산출한 계산값이다(AI 판단 아님).**
> 이 순위는 **"어디를 들여다볼 것인가"** 를 정하는 조사 대상 선정 지표다.
>
> **실제 실행 순서는 이와 다를 수 있다.** 실행 단계에서는 공식에 들어가지 않는 기준
> — 직접 타깃 규모(중분류 단위), 부정비율·Severe Negative 건수, 개발 난이도,
> 귀책 주체, 조직 역량 — 이 함께 고려되기 때문이다.
>
> **실행 우선순위는 [`output/action_plan_full.md`](output/action_plan_full.md) 하단 「우선순위 제안」에서 확인한다.**
>
> **실증 (3,000건):** 두 층위는 실제로 역전됐다.
>
> | 실행 우선순위 | 가설 | 소속 Topic의 Priority 순위 |
> |---|---|---|
> | **P1** | C-1 회수·환불 처리지연 | C = **3위** |
> | **P2** | A-1·A-2·A-3 배송지연 | A = **4위** |
> | P2 | I-1·I-2 제품 A/S | I = 1위 |
> | **P3** | D-1·D-2 주문접수 | D = **2위** |
>
> Priority 2위인 D의 실행안이 P3, Priority 4위인 A의 실행안이 P2가 됐다.
> 순위를 임의로 뒤집은 것이 아니라, **실행 단계의 잣대가 달라 자연히 갈린 결과**다.
>
> 관련: A(Priority 4위)는 §9의 대상 선정 규칙("S_score 최고 Topic 보정 추가")에 의해
> Root Cause 분석 대상에 포함됐다. 순위가 낮다고 분석에서 배제되지 않는다.

```
Priority = 0.4 × F_score + 0.4 × S_score + 0.2 × T_score
```

모든 항이 0~100이므로 Priority도 0~100이다.

**등급 (P1 / P2 / P3) — 확정**

```
정렬: Priority 내림차순
P1 = 상위 N개 이내  AND  Priority ≥ 50
P2 = P1이 아니면서  Priority ≥ 50
P3 = Priority < 50
N = 3
```

두 조건을 **모두** 만족해야 P1이다. 상위 3개여도 50점 미만이면 P1이 아니라 P3다.

**근거 (두 축이 독립적이다):**

| 기준 | 값 | 근거 |
|---|---|---|
| 역량선 N | 3 | **"CS 개선 TF가 한 사이클에 착수 가능한 과제 수"**라는 조직 운영 가정. 대응 못 할 개수를 P1로 찍으면 우선순위가 무의미해지므로 역량에서 상한을 건다. 원 설계 목업(P1 2건 + P2)과도 정합 |
| 하한선 | 50 | **공식의 중심점.** F/S/T가 모두 "평균 상태"(비중 10%, 감정 중립~부정 경계, 추세 보합)이면 Priority가 정확히 50이 된다. "평균적인 이슈 미만은 최우선이 될 수 없다"는 의미 기준선. 데이터를 보기 전에 선언하며 분포에 맞춰 되돌리지 않는다 |

> **왜 순수 절대(60/40)도, 순수 상대(상위 3개)도 아닌가:**
> - 순수 절대는 "왜 60인가"에 운영상 답이 없었다(임의값).
> - 순수 상대는 이슈가 전부 시시해도 억지로 P1을 3개 만든다.
> - 결합하면 **P1이 0~3개로 유동**한다. "이번 데이터에는 당장 착수할 만한 이슈가 없다(P1=0)"가 유효한 결과로 성립하며, 이는 앞서 확정한 원칙("기준을 먼저 정하고 결과가 마음에 안 들어도 바꾸지 않는다")과 일치한다.
> - 이는 ITIL 인시던트 관리(Impact×Urgency 하한 + 처리 역량)와 제품관리 RICE(점수 + 컷라인)가 실제로 쓰는 결합 구조다.

**데이터 한계 명시:** 베이스라인(과거 평균 Priority) 기반 하한이 이상적이나, 본 데모는 단일 스냅샷이라 시계열이 없다. 하한을 공식 중심점(50)으로 대체한 이유이며 README에 기재한다.

**N은 파라미터다.** 코드에서 상수로 두어 조직 역량 가정이 바뀌면 조정 가능하게 한다.

### 6.5 산출물 스키마 (Topic 단위)

| 필드 | 설명 |
|---|---|
| topic | Topic 명 |
| voc_count | 해당 Topic VOC 수 |
| frequency_pct | 원본 비중(%) — 대시보드 표시용 |
| f_score / s_score / t_score | 0~100 |
| severity_raw | 0~5 |
| trend_raw_pct | 원본 변화율(%) — 대시보드 표시용 |
| trend_insufficient | 표본 부족 플래그 |
| priority | 0~100 |
| priority_grade | P1 / P2 / P3 |

---

## 7. AI / Rule / Human 경계

**이 경계는 고정이다. AI가 Rule 영역을 침범해서는 안 된다.**

| 단계 | 담당 | 비고 |
|---|---|---|
| Topic 택소노미 발견 | AI (임베딩·클러스터링) + **Human 확정** | 최종 결정은 사람 |
| Topic 분류 | AI (LLM) | 고정 택소노미 기반 |
| Sentiment 분류 | AI (LLM) | 4단계 라벨 |
| Frequency 계산 | **Code** | |
| Severity 계산 | **Code** | 라벨→점수 환산 및 평균 |
| Trend 계산 | **Code** | |
| Priority 계산 | **Code** | |
| Root Cause 가설 | AI (LLM) | **Human Review 필수** |
| Recommendation | AI (LLM) | **Human Review 필수** |
| 최종 결정 | **Human** | |

> **금지:** LLM이 Priority 점수나 순위를 직접 산출하는 것. 모든 점수는 코드가 계산한다. LLM은 라벨만 부여한다.

### 모델 배분

| 작업 | 모델 | 근거 |
|---|---|---|
| Topic 분류, Sentiment 분류 | Haiku 4.5 | 대량 반복, 속도·비용 |
| Root Cause, Recommendation | Opus 5 | 소량, 추론 품질 |

배치 25건/호출, 병렬 8~10.

---

## 8. Domain Context 문서 4종

`context/` 하위에 작성한다. **§5 1단계에서 확정된 Topic 택소노미가 나온 뒤에 작성한다.**

| 파일 | 질문 | 사용 단계 |
|---|---|---|
| topic_taxonomy.md | 어떤 토픽이 있고 그 경계는 무엇인가 (10토픽 + Label Guideline: 정의·포함·제외) | Topic 분류, 검증 |
| process_map.md | 서비스가 어떻게 작동하는가 | Root Cause, Recommendation |
| cause_taxonomy.md | 어떤 원인이 존재하는가 | Root Cause, Recommendation |
| action_playbook.md | 어떤 대응을 할 수 있는가 | Recommendation |
| metric_dictionary.md | 개선 여부를 무엇으로 측정하는가 | Recommendation |

> `topic_taxonomy.md`가 먼저 확정되어야 100건 사람 라벨링(§12)과 LLM 분류가 시작될 수 있다. 작업 순서의 출발점이다.

### 필수 선언 (4개 문서 모두 상단에 기재)

> **본 프로젝트의 VOC는 AI Hub 공개 데이터이며, 업무 맥락(Process Map / Cause Taxonomy 등)은 해당 데이터에서 관찰된 패턴과 공개 자료를 근거로 추론하여 구성하였다. 실제 현업에서는 유관부서 협업, 기존 정책·프로세스 문서를 기반으로 PM이 구조화한다.**

### 작성 규칙

1. 각 항목에 근거를 표기한다 — 관찰된 voc_id 또는 소비자원 사례 번호
2. process_map.md는 **데이터에서 실제로 관찰된 단계만** 기술한다. 관찰되지 않은 내부 시스템·조직 구조를 상상해서 넣지 않는다
3. 근거를 댈 수 없는 항목은 넣지 않는다

> 원 설계의 원칙("실제 기업의 존재하지 않는 업무 프로세스를 상상해서 만들게 하면 안 된다")은 **현업 수행 시의 원칙**이며, 이 프로젝트에 그대로 적용되는 제약이 아니다. 공개 데이터에는 특정 기업의 실제 프로세스가 없으므로, 위 선언과 근거 표기로 추론했음을 명시하는 것이 이 프로젝트에서의 올바른 준수 방식이다.

### 현업 대응 문서 (README 기재용)

이 4종이 실제 현업에서 무엇을 대신하는지 밝혀 설계 의도를 분명히 한다.

| 본 프로젝트 문서 | 현업의 대응 문서 |
|---|---|
| process_map.md | 업무 프로세스 정의서(SOP), 시스템 구성도, 주문~배송 플로우차트 |
| cause_taxonomy.md | **반품·환불 사유 코드체계**, CS 처리이력 코드, 장애·인시던트 리포트, RCA 보고서 |
| action_playbook.md | CS 응대 매뉴얼, 이슈 에스컬레이션 기준, 개선과제 이력 |
| metric_dictionary.md | 전사 KPI 사전, CS 지표 정의서(FCR·CSAT·처리시간 등) |

> 특히 cause_taxonomy는 현업에 이미 존재하는 경우가 대부분이다(반품 사유 코드: 오배송·파손·단순변심 등). 없는 것을 새로 만드는 게 아니라, 흩어진 코드체계를 AI가 참조할 수 있는 형태로 구조화하는 작업이라는 점을 README에 밝힌다.

### metric_dictionary.md — 2계층 구조 (확정)

Business Impact를 공식에 넣지 않는 대신(§1-A), 지표 사전을 2계층으로 나눠 사업 임팩트로의 연결 지점을 문서에 남긴다.

| 계층 | 지표 예시 | 본 데모에서 |
|---|---|---|
| **1차 — VOC 관측 지표** | 토픽 VOC 비중, 부정 감정 비율, 재문의율 | **실제로 측정하고 대시보드에 표시** |
| **2차 — 사업 지표** | 재구매율, 반품률, CS 처리 건수·비용, 이탈률 | **측정하지 않음. 대응하는 1차 지표만 명시** |

각 1차 지표 항목에 대응하는 2차 지표를 병기한다. 예: *"배송 지연 VOC 비중 ↓ → (연결) CS 처리비용 ↓, 재구매율 ↑"*. Recommendation의 Success Metric(§9)은 **1차 지표에서만** 선택한다.

---

## 9. Root Cause / Recommendation 계약

### Root Cause

**대상 Topic 선정 (Priority 랭킹 + 강도 보정)**

Priority **상위 3개**를 기본으로 하되, **S_score 최고 Topic이 상위 3개에 없으면 1개 추가**한다.

> 이유: Priority는 F·S·T를 0.4:0.4:0.2로 합산하므로, **빈도는 낮지만 강도가 최고인 Topic이 랭킹에서 밀린다.**
> 3,000건 실측에서 A(배송지연)가 S_score 32.1·부정비율 27.5%로 양쪽 1위인데 Priority 4위(46.6)로 P1에서 탈락했다.
> "가장 화난 지점"을 원인 분석에서 빠뜨리지 않기 위한 보정. 사분면(09 대시보드) 없이 규칙 한 줄로 처리한다.
> → 3,000건 기준 대상: **I · D · C**(상위 3) **+ A**(S_score 최고) = 4개.

**입력 (3계층)**

- Layer 1: 해당 Topic의 대표 VOC 원문 8~12건
- Layer 2: 해당 Topic의 Frequency / Severity / Trend 결과
- Layer 3: process_map.md + cause_taxonomy.md

**출력**

| 항목 | 규칙 |
|---|---|
| Issue | Topic 명 |
| Observed Pattern | VOC에서 **직접 관찰된 사실**만 |
| Root Cause Hypothesis | **최대 3개.** 사실이 아니라 가설로 서술 |
| Evidence | 가설마다 **인용 voc_id 1개 이상 필수** |
| Human Review | 채택 / 수정 / 기각 |

**Evidence 검증 (필수 구현)**

```
LLM이 반환한 voc_id가 실제 데이터에 존재하는지 코드가 검증한다.
존재하지 않으면 해당 가설을 폐기한다.
```

> 공개 데이터에는 정답 원인이 없다. 이 검증 루프가 있어야 근거 기반이라 주장할 수 있다.

### Recommendation

**입력:** Root Cause 결과 + action_playbook.md + metric_dictionary.md

**출력:** Action / Expected Effect / Success Metric / **Deliverable(산출물 유형)**

Success Metric은 **metric_dictionary.md에 정의된 1차 지표 중에서만** 선택한다. 새 지표를 만들어내지 않는다.

**Deliverable 라우팅 — 귀책 주체가 결정한다 (사분면 아님)**

각 Root Cause 가설의 `cause_taxonomy.md` **귀책 태그**로 산출물 형식을 정한다. 문제 성격이 아니라 "누가 고칠 수 있는가"가 산출물을 결정한다.

| 귀책 태그 | 산출물(Deliverable) | 근거 |
|---|---|---|
| `[플랫폼/시스템]` | **Jira 티켓** (재현조건·기대동작·연관 VOC) | 내부 개발로 해결 |
| `[정보/매뉴얼]` | **Help Center / FAQ 개편안** | 콘텐츠 보강으로 해결 |
| `[판매자]` | **판매자 정책·가이드 개정안** | 셀러 정책·교육 영역 |
| `[택배사]` | **파트너 협의 안건** | 내부 티켓으로 해결 불가, 외부 협상 |
| `[고객]` | (산출물 없음 — 안내·UX 유도로 흡수) | 프로세스 결함 아님 |
| 효과 불확실 | **A/B 테스트 제안서** | 개선 방향이 둘 이상일 때 |

> **AI는 초안 스켈레톤까지만 만든다.** 각 산출물은 VOC 인용이 붙은 뼈대(제목·문제기술·근거·빈칸 목표)로 생성하고,
> **목표 수치·우선순위·범위는 사람이 채운다.** 완성된 PRD/티켓을 AI가 확정하면 환각 비용이 가장 큰 지점이 된다.
> MVP에서는 **산출물 유형당 예시 1개(exemplar)**만 실제 생성하고, 나머지는 "동일 템플릿으로 확장" 으로 남긴다.

**Expected Effect — Frequency 기반 정량화 (필수, Code 계산)**

사업 데이터 없이도 계산 가능한 유일한 정량 임팩트는 Frequency다. LLM이 서술하지 않고 **코드가 계산해서 채운다.**

```
Expected Effect = "이 이슈가 해소되면 전체 VOC 인입이 {frequency_pct}% 감소한다"
```

예: *"배송 지연 — 전체 VOC의 18%. 개선 시 VOC 인입 18% 감소 기대"*

이 수치는 추측이 아니라 관측값(§6.1 frequency_pct)이므로 근거 검증이 필요 없다. 유관부서는 자체 보유한 CS 문의 1건당 처리 비용을 곱해 즉시 비용 절감액으로 환산할 수 있다 — 그 환산은 본 프로젝트의 범위 밖이다(§1-A).

### Human Review 저장 (필수)

채택/수정/기각 결과를 `output/review_log.jsonl`에 append한다.

> UI 요소로만 존재하고 저장되지 않으면 Human-in-the-loop 주장이 성립하지 않는다.

---

## 10. 보류 항목 — Critical Issue Override

"1건의 개인정보 유출 vs 500건의 배송 지연"처럼, 발생량이 적어도 즉시 대응이 필요한 이슈는 선형 합산으로 포착되지 않는다. 실무에서는 override rule이 필요하다.

**MVP에서는 구현하지 않는다.** 범위 확대를 방지한다.

README의 Future Work 및 포트폴리오의 "설계 한계" 항목으로 기재한다. 한계를 인지하고 의도적으로 제외했음을 밝히는 것이 설계 역량의 근거가 된다.

---

## 11. 미결정 항목

**① Priority 등급 컷오프 — 확정 완료 (§6.4 참조)**

`P1 = 상위 3개 AND ≥50 / P2 = 나머지 ≥50 / P3 = <50`. 결합 기준으로 확정. 상세 근거는 §6.4.

**② Topic 개수 — 잔여**

8~12개 범위로 두었으나, §5 1단계 군집화 결과를 보고 사람이 확정한다. 이는 "데이터 사후 적합"이 아니라 관찰 사실의 확인이므로 사전 선언 대상이 아니다.

---

## 12. 검증

### 12.1 Topic·Sentiment 분류 — Human 검토 기반 평가 (주 검증)

> **위치: 파이프라인 06 게이트.** 분류(04·05) 직후, 지표 산출(07) **이전**에 통과해야 한다.
> 06의 Priority와 08의 원인가설이 모두 분류 라벨을 입력으로 받으므로, 검증이 파이프라인 끝에 있으면
> 문제를 발견해도 상류 산출물을 다시 만들어야 한다. 검증은 사후 보고서가 아니라 관문이다.

**방식: AI가 채운 라벨을 사람이 검토한다.** 사람이 백지에서 정답지를 먼저 만드는 방식이 아니라,
원문 + AI 라벨을 함께 놓고 **채택 / 수정** 을 판정한다. 후보 라벨이 있는 쪽이 판단 기준이 안정적이다.

> **산출 지표는 "검토 수정률"이다.** AI 답을 먼저 보므로 동조 편향이 있어, 블라인드 정확도와 동일하지 않다.
> 수치를 인용할 때 `정확도`가 아니라 `검토 수정률`로 표기한다.

**표본:** **100건.** 한 건에 Topic·Sentiment 두 라벨을 함께 검토한다.

**표본 추출: 층화.** 랜덤 100건은 작은 토픽(예: TV AS 2.5%)이 2~3건만 잡혀 측정 불가하다. **토픽당 최소 8~10건**을 보장하도록 층화 추출한다. 전체 수정률은 실제 분포로 가중 환산해 병기한다.

**측정 지표:**

| 대상 | 지표 |
|---|---|
| Topic 분류 (10종) | 전체 검토 수정률 + **토픽별 수정 방향**(어느 토픽에서 어느 토픽으로 옮겼는지) + confusion matrix |
| Sentiment 분류 (4종) | 전체 검토 수정률 + 라벨별 수정 방향 |

**약한 토픽 식별 (명시 산출물):**
- **precision 낮음** = 그 토픽에 과잉 할당(다른 걸 빨아들임)
- **recall 낮음** = 그 토픽을 놓침(다른 데로 샘)
- 두 실패 유형을 구분해 어느 토픽·어느 방향이 약한지 리포트한다. → Label Guideline 보정에 사용

**순서:** Label Guideline 확정(§5) → LLM 분류 → **사람 100건 검토(06 게이트)** → 어긋난 지점을 Guideline에 되먹임 → 지표 산출(07)로 진행. 사람과 LLM이 동일 Guideline을 봐야 판정이 공정하다.

### 12.2 보조·기타 검증

| 대상 | 방법 | 규모 |
|---|---|---|
| Topic 분류 (보조) | `src_topic`(원본 7종)과 대조 — 참고용 sanity check. 원본 7종 ≠ 택소노미 10종이므로 일치율은 참고치일 뿐 | 전량 |
| Sentiment (보조) | `src_speech_act`와 상관 확인 (지시/요구 ↔ Severe Negative) | 전량 |
| Trend 로직 | §4에서 심은 추세 정답값과 대조 | 전 Topic |
| Root Cause | 사람이 채택/수정/기각 | 대상 Topic 4개 (아래 선정 규칙) |
| Priority | 사람이 납득 가능성 평가 (상/중/하) | 전체 랭킹 |

**산출물:** confusion matrix, 약한 토픽 목록, 오류 사례와 개선 포인트를 README에 기재한다.

---

## 13. 구조

```
voc_insight_project/
├── app/
│   ├── main.py              # Streamlit 1페이지
│   ├── pipeline.py          # 전체 오케스트레이션
│   ├── loader.py            # 데이터셋 어댑터 (544 / 백업 전환점)
│   ├── cleaning.py          # 정제 + PII 마스킹 + 합성 타임스탬프
│   ├── clustering.py        # 택소노미 발견 (오프라인 1회)
│   ├── classify.py          # Topic 분류 (LLM)
│   ├── sentiment.py         # Sentiment 분류 (LLM)
│   ├── scoring.py           # F/S/T/Priority (Code only)
│   ├── root_cause.py        # 가설 + Evidence 검증
│   └── recommendation.py
├── context/                 # 도메인 문서 4종 + topic_taxonomy.md
├── data/
│   ├── voc_500.csv
│   ├── trend_ground_truth.json
│   └── README.md            # 합성 타임스탬프 고지
├── output/
│   ├── results.json         # 캐시된 분석 결과
│   └── review_log.jsonl
├── PROJECT_SPEC.md
└── README.md
```

### 데모 실행 모드

- **기본: 조회 모드** — `output/results.json`을 읽어 즉시 렌더링. API 키 없이 동작하므로 배포 링크가 항상 살아있다
- **재분석 모드** — 실제 파이프라인 실행 후 **실측 소요 시간을 표시**한다

### 분류 캐시 및 재개 (필수 — 확장성 대비)

표본 크기를 늘려 전량(15,329건) 실행을 대비한 최소 장치. 500건 개발 단계에서도 반복 실행 속도를 위해 동일하게 적용한다.

- **캐시:** `voc_id → {topic, sentiment}` 매핑을 `output/classify_cache.jsonl`에 저장한다. 재실행 시 이미 분류된 voc_id는 LLM 호출을 건너뛴다. 프롬프트 버전이 바뀌면 캐시를 무효화한다
- **재개:** 배치 단위로 결과를 즉시 append 저장한다. 호출 도중 중단되어도 처음부터 다시 돌리지 않고 마지막 저장 지점부터 이어간다

---

## 14. 대시보드 표시 사양

| 영역 | 내용 |
|---|---|
| 요약 | 전체 VOC 수 / 주요 이슈 수 / 부정 VOC 비율 |
| 랭킹 | Topic, Frequency(%), S_score, Trend(%), Priority, 등급 |
| 상세 | 주요 고객 표현, Root Cause 가설 3개 + Evidence, Recommendation, **개선 시 기대효과(§9)**, Priority 근거 분해 |

**개선 시 기대효과:** §9에서 계산한 Frequency 기반 정량 문구를 Recommendation 옆에 표시한다. 예: *"개선 시 VOC 인입 18% 감소 기대"*. 사업 임팩트 수치가 아니라 **관측된 비중**임을 라벨로 구분한다.

**Priority 근거는 F_score / S_score / T_score 와 각각의 가중 기여분으로 표시한다.**

> 원 설계 목업의 오류를 정정한다:
> - `Actionability 15` — MVP 제외 지표이므로 **삭제**
> - `Severity -72%` — 0~100 점수 체계와 불일치. **점수로 표기**
> - `Severity 16` — 가중 기여분(0.4 × S_score)과 원 점수를 혼용. **구분하여 표기**

Trend 컬럼 인근에 §4 고지 문구를 표시한다.

---

## 15. 현업 실제 활용 시 변경되는 부분

본 SPEC은 **MVP 데모의 구현 기준서**다. 현업에서 이 시스템을 지속 운영하는 프로그램으로 만들 경우 상당 부분이 달라진다. 그 전환 설계는 별도 문서로 분리했다.

> **→ [production_architecture.md](production_architecture.md)** — 현업 운영 전환 설계 (MVP vs 운영 비교, 4레이어 구조, 재사용성, 기술 요건)

**분리한 이유:** "지금 구현할 것"과 "구현하지 않기로 했으나 확장 경로는 아는 것"을 한 문서에 섞으면 구현 범위가 흐려진다. SPEC은 좁게 유지하고, 확장 설계는 참조로 연결한다.

### 주요 변경 지점 요약

| 영역 | 본 MVP | 현업 운영 | 상세 |
|---|---|---|---|
| 실행 모델 | 스냅샷 1회 배치 분석 | 상시 파이프라인 (인입 시 분류) | production §1 |
| Topic·Sentiment 분류 | 전량 LLM 호출 (§7) | **경량 분류기 상시 + LLM은 신규 유형·경계 케이스만** | production §3-② |
| 주문 상태값 | 미관측(10% 미만)으로 배제 (§4) | **주문 DB 조인**으로 해소 | production §3-① |
| Priority 공식 | `0.4F + 0.4S + 0.2T` (§6.4) | `0.3F+0.3S+0.2T+0.2B` — 사업 데이터로 **B 복원** (§1-A) | production §2 |
| 등급 하한선 | 공식 중심점 50 (베이스라인 부재, §6.4) | **과거 평균 베이스라인** 대비 | production §4 |
| Domain Context | 관찰 기반 **추론** 작성 (§8) | **유관부서 협의**로 정제 | production §5 |
| Human Review | 1회 100건 검증 (§12) | **주간 샘플 검토 상시 루틴** | production §4 |
| 피드백 | 문제 발견에서 종료 | **개선 실행 → 효과 측정** 순환 | production §4 |

### 택소노미 재사용성 (설계 시점에 확보)

대분류 10개는 **커머스 공통 코어 7 + 업종·채널 확장 슬롯 3**으로 분리 설계했다.

- **코어 7 (업종 무관):** A 배송 / B 취소 / C 반품·교환·환불 / E 절차·정책 / F 결제 / G 혜택·적립 / H 계정·회원
- **확장 3 (교체 대상):** D 상품 주문 접수(채널 특화), I 제품 A/S·J 제품 사용법(업종 특화)

다른 커머스 이식 시 코어는 유지하고 확장 슬롯만 교체한다(패션 → 사이즈·소재, 식품 → 신선도·보관 등). 상세: production §6.

### 기술 스택에 대한 입장

production 문서는 **각 레이어의 요건(requirement)을 확정하고 기술은 후보로만 제시**한다. 요건 정의는 서비스 운영 판단으로 본 설계의 범위이나, 제품·프레임워크 선택은 트래픽·기존 인프라·팀 역량에 따라 달라지는 엔지니어링 결정 영역이기 때문이다. 상세: production §7.
