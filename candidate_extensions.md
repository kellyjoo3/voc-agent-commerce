# candidate_extensions.md — 미검증 관찰 기록

> **이 파일은 미검증 관찰 기록입니다.**
> `configs/commerce_cs.json`에 반영되지 않았습니다.
> **확장 마법사 사용 시 참고 사례로만 활용하세요.**
>
> 정식 카테고리로 인정받으려면 `EXTENDING.md` 체크리스트 6개 필드를 모두 채워야 합니다.
> 완료 후 `configs/local_extensions.json`에 저장됩니다.

---

## 관찰 배경

이전 분석 세션(합성 50건 테스트)에서 아래 카테고리 후보가 관찰되었습니다.
해당 세션은 원본 context/ 파일 없이 진행되었고, 테스트 데이터(합성 50건)만으로
`configs/commerce_cs.json`을 수정한 것이 이관 위반으로 판정되어 격리 조치되었습니다.

아래는 그 세션의 관찰 기록을 이관 위반 없이 보존한 것입니다.

---

## 후보 K — 재고·재입고 문의

### 관찰 근거 (합성 데이터에서 발견, 원본 미확인)

이전 세션에서 합성 50건 처리 시 "재고 있나요", "언제 다시 들어오나요" 유형의 발화가
"기타"로 분류되었습니다.

### 현재 상태

| 체크리스트 필드 | 상태 |
|---|---|
| 1. 정의 | 미작성 |
| 2. 포함 예시 (원본 데이터 원문 인용) | 미작성 — 합성 데이터 원문은 검증 불가 |
| 3. 제외 항목 + 행선지 | 미작성 |
| 4. 귀책태그 매핑 | 미작성 |
| 5. 대응 Action 최소 1개 | 미작성 |
| 6. 연결 Metric | 미작성 |

**→ 6개 필드 모두 미완성. 정식 카테고리 인정 불가.**

### 활용 방법

실제 CSV를 처리할 때 "재고·입고" 관련 발화가 "기타"로 많이 나오면,
확장 마법사에서 이 후보를 참고 삼아 원본 데이터의 실제 발화를 인용해
체크리스트를 새로 작성하면 됩니다.

---

## 후보 L — 기술적 장애·앱 오류

### 관찰 근거 (합성 데이터에서 발견, 원본 미확인)

이전 세션에서 합성 50건 처리 시 "앱이 안 켜져요", "로딩이 안 됩니다" 유형의 발화가
"기타"로 분류되었습니다.

### 현재 상태

| 체크리스트 필드 | 상태 |
|---|---|
| 1. 정의 | 미작성 |
| 2. 포함 예시 (원본 데이터 원문 인용) | 미작성 — 합성 데이터 원문은 검증 불가 |
| 3. 제외 항목 + 행선지 | 미작성 (F2 결제오류와 경계 미정의) |
| 4. 귀책태그 매핑 | 미작성 |
| 5. 대응 Action 최소 1개 | 미작성 |
| 6. 연결 Metric | 미작성 |

**→ 6개 필드 모두 미완성. 정식 카테고리 인정 불가.**

### 주의 사항

F2(결제 실패·오류)는 이미 원본 택소노미에 "결제가 진행되지 않음"을 포함하고 있습니다.
앱/시스템 오류 중 결제와 무관한 유형이 실제 데이터에서 유의미하게 나타나는지
먼저 확인이 필요합니다.

---

## 이 기록의 한계

- 합성 50건은 통계적으로 무의미하며, 실제 VOC 분포를 대표하지 않습니다.
- 합성 데이터의 원문은 검증된 실제 발화가 아니므로 체크리스트 항목 2(포함 예시)에 사용할 수 없습니다.
- 위 후보들은 "기타"가 많이 나온 관찰 결과일 뿐이며, 실제 데이터에서 동일한 패턴이
  나타나리라는 보장이 없습니다.
