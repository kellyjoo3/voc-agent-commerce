# PLAYBOOK.md — VOC 분석 실행 지시문 (v2.0)

> **당신(호스트 AI)에게:** 이 문서는 사용자가 제공한 커머스 CS VOC CSV를 분석하기 위해
> 당신이 그대로 따라야 하는 절차다. 이 리포에는 실행 가능한 프로그램이 없다 —
> **당신이 각 단계를 직접 수행하는 것이 이 도구의 전부다.**
>
> **지켜야 할 세 가지 원칙**
> 1. **의미 판단(분류·감정·원인가설·실행안)은 당신이 직접 수행한다.** 외부 API를 새로
>    호출하거나 프롬프트를 별도로 구성할 필요 없다 — 당신 자신의 판단력을 사용하라.
> 2. **계산(F/S/T·Priority 등 확정 수식)은 절대 암산하지 마라.** 반드시
>    `scripts/priority_calc.py`를 실행해서 처리한다. 암산한 숫자를 결과로 제시하면
>    이 도구의 신뢰성 전체가 무너진다.
> 3. **Human 개입 지점은 실제로 멈춰라.** 아래 "🛑 STOP" 표시가 있는 단계에서는
>    사용자의 명시적 응답을 받기 전까지 다음 단계로 진행하지 않는다.

---

## 단계 번호 체계 — 원본 12단계 매핑

| 단계 | 이 도구에서 | 담당 | 비고 |
|---|---|---|---|
| **00** CSV 스키마 매핑 | ✅ 런타임 🛑 STOP | AI+Human | 원본에 없던 신규 단계 — 사용자 CSV가 다양하므로 필수 |
| **01** 데이터 수집·정제 | ✅ 런타임 | AI | 도메인 필터를 하위 절차로 포함 (SPEC §3) |
| **02** 층화 표본 추출 | ⚠️ 조건부 | AI | 입력 5,000건 초과 시만. 소규모면 전량 처리 |
| **03** 임베딩·군집화 | ❄️ 동결 | — | 결과물: `configs/commerce_cs.json`의 `taxonomy` |
| **04** 택소노미 확정+Guideline | ❄️ 동결 | — | 결과물: `taxonomy.major` A~J 10개 + Guideline |
| **05** Domain Context 4종 | ❄️ 동결 | — | 결과물: `cause_tags`, `playbook.actions`, `metrics`, `filters` |
| **06** Topic 분류 | ✅ 런타임 | AI | 대분류 A~J + 중분류(null 허용) |
| **06.5** 확장 마법사 | ✅ 런타임 🛑 STOP | AI+Human | 기타 15% 초과 시 발동 |
| **07** Sentiment 분류 | ✅ 런타임 | AI | 4단계 Positive/Neutral/Negative/Severe Negative |
| **08** 분류 결과 검토 | ✅ 런타임 🛑 STOP | Human | 표본 검토 게이트 |
| **09** 지표·우선순위 계산 | ✅ 런타임 | Code | `scripts/priority_calc.py` 실행 (암산 금지) |
| **10** 원인 가설 생성·검증 | ✅ 런타임 🛑 STOP | AI+Human | cause_taxonomy 기반 가설 + 채택 게이트 |
| **11** 실행안·Deliverable | ✅ 런타임 | AI | action_playbook 기반 실행안 초안 |
| **12** 검증·대시보드 | ✅ 런타임 | AI | 결과 요약 + 대시보드 |

> **동결 단계(03·04·05)** 는 원본 프로젝트에서 이미 완료된 작업이다.
> 이 도구를 사용하는 제3자는 이 단계를 재실행하지 않는다 — 결과물이 config 파일로 패키징되어 있다.

---

## 사전 준비 — 이 리포에서 읽어야 할 파일

작업을 시작하기 전에 아래를 읽어라.

- [`configs/commerce_cs.json`](configs/commerce_cs.json) — 택소노미(대분류 A~J 10개), Label Guideline,
  도메인 필터 패턴, Sentiment 척도 및 규칙, Priority 공식, 귀책태그·Action 카탈로그, 지표 정의.
  **이 config가 이 도구의 두뇌다.** 여기 정의를 벗어나 임의로 카테고리를 만들지 마라.
- [`configs/local_extensions.json`](configs/local_extensions.json) — 사용자가 추가한 확장 카테고리.
  `extensions` 배열이 비어 있으면 건너뛰어도 된다. **00단계에서 두 파일을 병합 로드한다.**
- `context/` 폴더 — `cause_taxonomy.md`(원인 가설 단계 참조), `process_map.md` 등.
- `scripts/priority_calc.py` — 09단계 계산 전용 스크립트. `--help`로 사용법 확인.

---

## 00단계 — CSV 스키마 확인 및 컬럼 매핑 🛑 STOP

**목적:** 사용자의 CSV가 어떤 컬럼 구조든, 이 파이프라인이 필요로 하는 필드
(`voc_id`, `text`, `created_at`)로 규칙 기반 매핑을 제안하고 **사용자 확인 후에만** 진행한다.

### 🛑 먼저 — 분석 대상 파일이 지정되었는가

**사용자가 분석할 파일을 명시하지 않았다면 반드시 물어보고 답을 받은 뒤 진행한다.**

이 리포에는 `examples/sample_voc_50.csv`가 들어 있으나, 이는 **형식 참고용 합성 데이터**다.
사용자가 명시적으로 지정하지 않았다면 **이 파일을 기본 분석 대상으로 삼지 마라.**
리포에 CSV가 하나뿐이라는 이유로 그것을 분석하면, 사용자는 원하지도 않은 가짜 데이터
리포트를 받고 파이프라인이 불필요하게 한 번 더 도는 낭비가 발생한다.

물어볼 때는 이렇게 한다:

```
분석할 파일 경로를 알려주세요. (CSV 또는 xlsx)

시험 삼아 먼저 돌려보고 싶으시면 examples/sample_voc_50.csv 를 쓸 수 있습니다.
다만 이는 합성 데이터이므로 실제 의사결정 근거로 쓸 수 없습니다.
```

사용자가 샘플로 진행하기를 선택했다면, 최종 리포트의 `meta.caveat`에
**"합성 예시 데이터 기반 — 실제 의사결정 근거로 사용 불가"**를 반드시 넣는다.

### Config 로드

두 파일을 병합 로드한다:
1. `configs/commerce_cs.json` (원본 택소노미 + 도구 설정)
2. `configs/local_extensions.json` (사용자 확장, `extensions` 배열이 비어 있으면 skip)

`local_extensions.json`의 `status: "confirmed"` 및 `status: "draft"` 항목 모두 분류에 사용한다.
단, `status: "draft"` 항목으로 분류된 VOC는 리포트에서 '잠정' 배지로 표기한다.

### 지시

1. 사용자가 지정한 CSV 파일을 읽고, 헤더(컬럼명)와 상위 3~5개 행의 샘플 값을 확인한다.
2. 아래 규칙으로 각 필드의 매핑 후보를 자동 추정한다.

   | 필요 필드 | 매핑 규칙 (대소문자 무시, 부분일치 허용) |
   |---|---|
   | `voc_id` | 컬럼명에 `id`, `번호`, `no`, `순번`이 포함된 컬럼. 없으면 "행 번호를 자동 생성하겠다"고 제안 |
   | `text` | 컬럼명에 `text`, `내용`, `문의`, `본문`, `content`, `message`, `review`가 포함된 컬럼. 여러 후보가 있으면 가장 텍스트 길이가 긴 컬럼을 우선 제안 |
   | `created_at` | 컬럼명에 `date`, `일자`, `시간`, `time`, `at`가 포함되고, 샘플 값이 날짜로 파싱되는 컬럼. **없어도 진행 가능** (Trend 계산만 생략됨을 고지) |

3. 매핑 후보와 각 컬럼의 샘플 값 2~3개를 표로 사용자에게 제시하고 확인을 요청한다:

   ```
   다음과 같이 컬럼을 매핑하려 합니다:

   | 필요 필드 | 매핑된 원본 컬럼 | 샘플 값 |
   |---|---|---|
   | voc_id | 고객id | 1001, 1002, 1003 |
   | text | 문의내용 | "배송이 너무 늦어요...", ... |
   | created_at | 작성일 | 2026-03-02, 2026-03-02, ... |

   이대로 진행할까요? [Y] 진행 / [n] 다시 추정 / 특정 필드를 수동으로 지정하려면
   "text는 customer_message 컬럼으로" 와 같이 알려주세요.
   ```

4. **⚠️ 함정 주의:** `인입 카테고리` 또는 이미 분류된 것처럼 보이는 컬럼이 있어도
   절대 이것을 분류 결과로 쓰거나 정답지로 사용하지 마라. 우리 택소노미(A~J)로
   `text`(문의내용)를 독립적으로 분류해야 한다.

5. **🛑 STOP — 사용자 응답 없이 다음 단계로 넘어가지 않는다.**
   - `Y`(또는 동의 표현) → 01단계로 진행
   - `n` → 매핑 규칙을 재검토하거나, 후보가 없던 필드에 대해 직접 질문
   - 특정 필드 수동 지정 → 해당 필드만 교체하고 다시 표를 보여준 뒤 재확인

6. `text` 컬럼을 찾지 못하면 진행할 수 없다. 사용자에게 직접 물어라.
   `created_at`을 찾지 못해도 진행은 가능하다 — 단, "Trend(추세) 지표는 계산되지 않으며
   T_score는 전부 중립값(50)으로 처리됩니다"라고 고지한다.

**금지:** 매핑을 추정 없이 사용자에게 통째로 떠넘기지 마라. 반대로, 확인 없이 임의로
확정해 다음 단계로 진행하지도 마라.

---

## 01단계 — 데이터 정제 (도메인 필터 포함)

**목적:** 분석 불가능한 행을 걸러내고, 텍스트를 분류에 적합한 형태로 정리한다.
도메인 적합성 사전 필터(원본 SPEC §3)도 이 단계에서 수행한다.

### 1-A. 기본 정제

1. `voc_id` 중복 제거 (첫 값만 유지, 제거 건수 기록).
2. `text`가 빈 값이거나 공백만 있는 행 제거 (제거 건수 기록).
3. `text` 완전 중복 행 제거 — **단, 반드시 같은 `voc_id`가 같은 텍스트를 반복 제출한
   경우에만 중복으로 처리한다.** `voc_id`가 다른데 `text`만 같은 경우(예: 여러 고객이
   동일 문구로 문의)는 **삭제하지 마라** — 이는 반복 이슈의 신호이며, 삭제하면
   Frequency 지표가 왜곡된다.
4. 과도하게 짧은 텍스트(5자 미만 기준, 다른 기준을 쓴다면 사용자에게 고지)는
   삭제하지 말고 `too_short` 플래그만 남겨 건수를 보고한다.
5. 불필요한 공백·줄바꿈은 정리하되, 느낌표 반복·감탄사·부정 표현은 감정 신호이므로
   그대로 둔다. 가명처리 토큰(`#@이름#`, `#@전번#` 등)은 절대 제거하지 않는다.
6. 잔존 PII 패턴(전화번호, 계좌번호 등 정규식으로 식별 가능한 패턴) 건수만 세어
   사용자에게 보고한다. 이 단계에서 자동 마스킹하지 않는다.

### 1-B. 도메인 적합성 사전 필터

`configs/commerce_cs.json`의 `filters.exclude_patterns`를 읽는다.
각 카테고리의 정규식 패턴(Python `re` 모듈 호환)으로 VOC `text`를 검사한다.

1. 패턴에 매치되는 행은 삭제하지 않는다. `domain_flag` 컬럼에 카테고리명을 태그한다.
2. 키워드가 부분적으로만 겹치거나 문맥상 애매하면 **여기서 판단하지 말고 통과시킨다** —
   애매한 잔여건은 06단계 Topic 분류에서 "기타"로 자연스럽게 처리한다.
3. `domain_flag`가 붙은 행은 06~09단계(분류·계산)에서 제외한다(삭제가 아니라 제외).
4. 격리 비율이 5%를 초과하면 사용자에게 경고한다: "이커머스 외 데이터가 예상보다 많습니다.
   CSV 출처를 확인해보시겠어요? (원 프로젝트 실측 정상 범주: 1.03%)"

### 산출

정제 결과 요약을 출력한다: 원본 건수 → 최종 건수, 각 사유별 제거/플래그 건수.

이 단계 결과는 `output/01_cleaned.csv`로 저장한다
(표준 컬럼: `voc_id, text, created_at, domain_flag`).

---

## 02단계 — 층화 표본 추출 (조건부)

**조건:** 입력이 5,000건을 초과할 때만 실행. 5,000건 이하면 이 단계를 건너뛰고 전량 처리한다.

**목적:** 반복 개발 속도를 위해 대용량 데이터를 대표 표본으로 축소한다.

### 지시 (5,000건 초과 시)

1. 대분류(또는 원본 CSV에 카테고리 컬럼이 있으면 그것)를 기준으로 **층화 추출**한다.
   층화 기준이 없으면 균등 무작위 추출.
2. 표본 크기 목표: 500~1,000건. 토픽별 Trend 계산을 위해 토픽당 최소 10건이 확보되도록
   층화 비율을 조정한다.
3. 시드를 고정해 재현성을 보장한다 (사용자에게 사용한 시드를 알려준다).
4. 표본 추출 결과를 보고한다: 원본 건수, 표본 건수, 층화 기준, 시드.

**5,000건 이하:** "입력이 N건으로 소규모이므로 전량 처리합니다" 라고 한 줄 고지하고
이 단계를 건너뛴다.

---

## 03단계 — ❄️ 동결 (임베딩·군집화)

> **이 단계는 동결됩니다.** 실행하지 않는다.
>
> **결과물:** `configs/commerce_cs.json`의 `taxonomy` 섹션.
> 원본 프로젝트에서 전량 15,163건의 임베딩·군집화(K=12)를 완료하고
> 사람이 원문 검토 후 2계층으로 재설계한 결과가 config에 패키징되어 있다.

---

## 04단계 — ❄️ 동결 (택소노미 확정+Guideline)

> **이 단계는 동결됩니다.** 실행하지 않는다.
>
> **결과물:** `configs/commerce_cs.json`의 `taxonomy.major` — 대분류 A~J 10개와
> 각 대분류의 정의·포함·제외(행선지) Guideline, 경계 판정 규칙 8개.
> 원본 프로젝트에서 사람이 검토·확정한 것이다. 이 도구를 사용하는 제3자는
> 이 결과를 그대로 사용한다.

---

## 05단계 — ❄️ 동결 (Domain Context 4종)

> **이 단계는 동결됩니다.** 실행하지 않는다.
>
> **결과물:** `configs/commerce_cs.json`의 아래 4개 섹션:
> - `filters.exclude_patterns` — 도메인 적합성 사전 필터 (8개 카테고리 정규식)
> - `cause_tags.tags` — 귀책 주체 태그 6개
> - `playbook.actions` — Action 카탈로그 28개
> - `metrics.primary` — 1차 지표 7개
>
> 원본 `context/` 폴더의 4종 문서(cause_taxonomy.md, action_playbook.md,
> metric_dictionary.md, domain_filter.py)에서 이관·구조화된 것이다.

---

## 06단계 — Topic 분류 (의미 판단 — 당신이 직접 수행)

**목적:** 각 VOC를 `configs/commerce_cs.json`의 고정 택소노미로 대분류(필수)·중분류(선택)
분류한다. **이것은 계산이 아니라 판단이다 — 스크립트를 실행하지 말고 당신이 직접 읽고 분류하라.**

### 지시

1. `configs/commerce_cs.json`의 `taxonomy` 섹션을 읽는다:
   - `taxonomy.classification_rules` — 판정 우선순위 3축 및 분류 규칙
   - `taxonomy.boundary_rules` — 경계 판정 규칙 8개 (필수 숙지)
   - `taxonomy.major[*].guideline` — 각 대분류의 definition·exclude(+route_to)

   **원본 Guideline(context/topic_taxonomy.md)의 §2, §4도 직접 참조할 것:**
   - §2 "공통 분류 규칙": 판정 우선순위 3단계("고객의 실제 요청 → 사유 → 사유 불명이면 null")와
     대분류 판별 3축(되돌리려는가 / 받았는가 / 내 건인가)
   - §4 "경계 판정 규칙": 확정된 8개 경계 규칙 (배송↔취소, 교환↔A/S, 절차문의 독립 등)

2. `domain_flag`가 붙은 행은 원칙적으로 Topic을 매기지 않고 넘어간다 (분류 대상 제외).
   명백히 커머스 CS 내용이 섞여 있다면 판단은 자유다.

3. `local_extensions.json`의 `status: "confirmed"` 확장 카테고리가 있으면,
   `commerce_cs.json` taxonomy와 **동등하게** 분류 후보로 사용한다.

4. 각 VOC에 대해:
   - 대분류(A~J 또는 `기타`) 하나를 배정한다 (필수)
   - 중분류(`minor`)는 사유가 텍스트에 명확히 드러날 때만 배정 (선택, 불명확하면 null)
   - `exclude` 항목에 걸리는 표현은 반드시 명시된 `route_to` 대분류로 보낸다

5. 대량 CSV 처리 시 20~30건씩 배치로 진행하고, 각 배치 완료마다 진행률을 짧게 보고한다
   (예: "30/50건 분류 완료").

### 산출

이 단계 결과는 `output/06_classified.csv`로 저장한다:

```
voc_id, text, created_at, domain_flag, topic, topic_minor
```

분류 완료 후 대분류별 건수 분포를 표로 요약해서 보여준다.
**"기타" 비율을 계산해서 표시한다** — 06.5단계(확장 마법사) 발동 조건 판단에 사용된다.

---

## 06.5단계 — 확장 마법사 🛑 STOP

**목적:** "기타" 비율이 높을 때 사용자가 안전하게 택소노미를 확장할 수 있도록 안내한다.

### 발동 조건

06단계 완료 후, **"기타" 비율이 15%를 초과하면 이 단계가 자동으로 시작된다.**
15% 이하면 이 단계를 건너뛰고 07단계로 진행한다.

### 발동 시 지시

1. **"기타" 비율 감지 및 알림:**

   ```
   현재 "기타" 비율이 XX%로 15% 임계값을 초과했습니다.
   이는 현재 택소노미(A~J)가 커버하지 못하는 문의 유형이 있다는 신호입니다.

   "기타"로 분류된 VOC 중 대표 사례를 보여드리겠습니다:
   - voc_id=N: "[원문 그대로]"
   - voc_id=N: "[원문 그대로]"
   - voc_id=N: "[원문 그대로]"
   (총 X건 중 대표 3~5건)

   새 카테고리를 추가하시겠어요?
   [Y] 확장 마법사 시작  /  [n] "기타"로 유지하고 07단계 진행
   ```

2. **🛑 STOP — 사용자 응답 대기.**
   - `n` → "기타"를 그대로 유지하고 07단계로 진행. "기타"가 높아도 이것은 버그가 아니다.
   - `Y` → 아래 마법사 절차 시작

3. **확장 마법사 절차 (Y 선택 시):**

   **EXTENDING.md의 체크리스트 6개 필드를 한 번에 하나씩 진행한다.**
   AI가 전체 필드를 한꺼번에 채워서 제안하는 것은 절대 금지다 — 이전 세션 실패의 재발 방지.

   ```
   [필드 1/6] 정의 — 이 카테고리가 다루는 것이 무엇인지 한 문장으로 작성해주세요.
   (참고: 위 "기타" 사례들을 보면 어떤 공통 유형이 보이시나요?)
   ```

   사용자가 필드 1을 작성하면:

   ```
   [필드 2/6] 포함 예시 — 지금 처리 중인 CSV에서 이 카테고리에 해당하는
   원문을 최소 2건 인용해주세요. (voc_id와 함께)
   ```

   이런 식으로 필드 2 → 3 → 4 → 5 → 6 순서로, **매 필드마다 멈추고 사용자 입력을 받는다.**

   **각 필드에서 AI의 역할:**
   - 사용자가 입력한 내용이 EXTENDING.md 기준에 맞는지 확인
   - 기존 A~J와의 경계 충돌이 있으면 즉시 알림 (예: "이 유형은 J1과 겹칠 수 있습니다. 제외 항목에 J1로의 경계를 추가해주세요")
   - 필드 내용이 불명확하면 "더 구체적으로 작성해주시겠어요?" 재질문

4. **6개 필드 완성 후:**

   ```
   새 카테고리 검토 결과:
   [작성된 6개 필드 요약]

   이대로 local_extensions.json에 저장할까요?
   [Y] 저장 후 07단계 진행  /  [n] 수정
   ```

5. **🛑 STOP — 저장 전 최종 사용자 확인.**
   - `Y` → `configs/local_extensions.json`의 `extensions` 배열에 추가.
     **저장 시 status 자동 판정:**
     - include_examples 중 `source="real"` 2건 이상 → `status: "confirmed"`
     - `source="real"` 2건 미만 → `status: "draft"` 로 저장하고 사용자에게 고지:
       "실발화 근거 N건으로 confirmed 저장 불가. draft로 저장합니다.
        분류에는 사용되나 리포트에서 '잠정' 배지로 표기됩니다."
     **`configs/commerce_cs.json`은 절대 수정하지 않는다.**
   - `n` → 수정할 필드를 물어보고 해당 필드만 다시 진행

6. **추가 확장 여부 확인:**

   ```
   저장 완료. 다른 "기타" 유형도 추가하시겠어요?
   [Y] 추가 카테고리 계속  /  [n] 07단계로 진행
   ```

7. 사용자가 거부하면 "기타"를 그대로 유지하고 07단계 진행. 거부는 정상 흐름이다.

---

## 07단계 — Sentiment 분류 (의미 판단 — 당신이 직접 수행)

**목적:** 각 VOC에 4단계 Sentiment를 부여한다. **이것도 계산이 아니라 판단이다.**

### 지시

1. `configs/commerce_cs.json`의 `sentiment_scale`과 `sentiment_classification_rule`을 읽는다:
   - `Positive` (score=0): 긍정적 또는 중립적 문의. 불만·요구 없음.
   - `Neutral` (score=1): 사실 확인·단순 문의. 감정 중립.
   - `Negative` (score=3): 불만·불편·실망. 해결 요구.
   - `Severe Negative` (score=5): 강한 항의·분노·반복 불만. 격앙된 표현.

2. **인사말 제외 규칙을 반드시 지켜라:**
   "안녕하세요", "감사합니다", "수고하세요" 같은 시작·종료 인사는 감정 판단에서 무시하고,
   문의·불만의 본문 내용만으로 판단한다.
   이 규칙을 누락하면 대부분의 VOC가 Positive로 오분류된다 (PROJECT_SPEC.md §6.2 실측 경고).

3. `domain_flag`가 붙은 행은 Sentiment를 매기지 않는다.

4. 각 VOC에 4단계 중 하나를 부여한다. Topic 분류와 마찬가지로 배치 단위로 진행하고
   진행률을 보고한다.

### 산출

이 단계 결과는 `output/07_sentiment.csv`로 저장한다:

```
voc_id, text, created_at, domain_flag, topic, topic_minor, sentiment
```

(06단계 산출물에 `sentiment` 컬럼을 추가한 파일이다.
이 파일이 09단계 `priority_calc.py`의 입력으로 사용된다.)

---

## 08단계 — 분류 결과 검토 🛑 Human 게이트

**목적:** 사람이 분류 표본을 직접 확인해 AI 분류의 신뢰성을 검증한다.

### 지시

1. `output/07_sentiment.csv`에서 각 대분류별로 3~5건씩 무작위 표본을 추출해 사용자에게 제시한다.
   아래 형식으로 보여준다:

   ```
   [A. 배송 지연·미도착] 표본 3건:
   voc_id=12 / topic=A / topic_minor=A1 / sentiment=Negative
   텍스트: "..."

   voc_id=34 / topic=A / topic_minor=null / sentiment=Neutral
   텍스트: "..."

   [수정이 필요한 항목이 있으면 알려주세요]
   ```

2. 전체 표본 검토 후:

   ```
   분류 결과를 채택하시겠어요?
   [Y] 채택 — 09단계 진행
   [수정] 수정이 필요한 voc_id와 올바른 분류를 알려주세요
   ```

3. **🛑 STOP — 사용자 응답 없이 09단계로 넘어가지 않는다.**

4. 수정이 있으면 해당 행을 수정하고 `output/07_sentiment.csv`를 갱신한 뒤 재확인한다.

5. 채택 후 "검토 수정률(수정 건수 / 전체 표본 건수)"을 보고한다.

---

## 09단계 — 지표·우선순위 계산 (스크립트 실행 — 암산 금지)

**목적:** F/S/T/Priority를 계산한다. **절대 암산하지 말고 스크립트를 실행하라.**

### 지시

아래 명령을 실행한다:

```bash
python scripts/priority_calc.py \
  --input output/07_sentiment.csv \
  --config configs/commerce_cs.json \
  --output output/09_priority.csv
```

스크립트 실행 결과를 그대로 사용자에게 보여준다.
출력에 포함된 안내 문구(Priority ≠ 실행순서, S_score 최고 Topic 별도 추가 여부)를
함께 전달한다.

**절대 이 계산을 암산하거나 근사치로 제시하지 마라.** 스크립트가 정상 실행되지 않으면
오류 메시지를 사용자에게 보여주고 해결을 돕는다. 오류 없이 계산된 숫자만 사용한다.

**소규모 데이터 주의 (T_score):**
수백 건 미만 데이터에서는 `trend_insufficient=True`가 대부분 토픽에 발생하며,
이 경우 Priority는 사실상 F·S 2항으로만 결정된다.
결과를 사용자에게 제시할 때 반드시 고지하라:
"대부분의 토픽에서 Trend 산출 불가(표본 부족). Priority 순위를 추세 근거로 해석하지 말 것."

### 🛑 진행 차단 조건 — 아래를 충족하지 못하면 10단계로 넘어가지 않는다

**1. 스크립트 실행 출력 원문을 사용자에게 그대로 보여줬는가?**

요약이나 재작성이 아니라 터미널 출력 그대로여야 한다. 보여줄 출력이 없다는 것은
스크립트를 실행하지 않았다는 뜻이며, 그 경우 이후 모든 수치가 무효다.

**2. F_score 검산 — 토픽 하나를 골라 손으로 확인하라**

F_score는 `min(비중% / 20, 1) × 100`이다. **20%가 상한(cap)이며, 최대값 대비
상대비율이 아니다.** 비중이 20%를 넘으면 전부 100이다.

| 비중 | 올바른 F_score |
|---|---|
| 53% | 100 (상한 도달) |
| 24% | 100 (상한 도달) |
| 13% | 65.0 |
| 9% | 45.0 |

검산 결과가 스크립트 출력과 다르면 **스크립트를 실행하지 않고 자체 계산한 것**이다.
즉시 중단하고 스크립트를 실행하라.

> **실제 재현 테스트 실패 사례:** 비중 24%인 토픽에 F_score 45를 부여한 실행이 있었다.
> 이는 `24 / 53 × 100`(최대값 대비 상대비율)로 계산한 것으로, SPEC 공식이 아니다.
> 올바른 값은 100이다.

**3. 등급을 임의로 조정하지 않았는가?**

`P1 = 상위 3개 AND Priority ≥ 50`. 심각도가 높다거나 리스크가 크다는 이유로
등급을 올리지 않는다. 그런 판단이 필요하면 **등급은 그대로 두고 별도 코멘트로** 적는다.

### 산출

`output/09_priority.csv` 컬럼:
```
topic, voc_count, frequency_pct, f_score, s_score, t_score,
severity_raw, trend_raw_pct, trend_insufficient, priority, priority_grade
```

---

## 10단계 — 원인 가설 생성·검증 🛑 Human 게이트

**목적:** Priority 상위 Topic에 대해 원인 가설을 생성하고 사용자 채택을 받는다.

### 대상 선정 (PROJECT_SPEC §9 규칙)

- `priority_grade = P1` 전체
- `priority_grade = P2` 전체
- S_score 최고 Topic이 P1/P2에 없으면 별도 추가

### 지시

1. 대상 Topic별로 `context/cause_taxonomy.md`를 읽고 원인 가설을 생성한다:
   - 해당 Topic 섹션의 원인 가설 목록에서 선택·조합한다
   - 각 가설에 **실제 voc_id를 인용한다** (근거 없는 가설은 제시하지 않는다)
   - 목록에 없는 원인을 지어내지 않는다

2. 각 Topic별 가설을 사용자에게 제시하고 채택/수정/기각을 요청한다:

   ```
   [Topic A: 배송 지연·미도착] 원인 가설

   가설 A-1: 판매자 출고 지연 [판매자]
   근거: voc_id=N "...", voc_id=N "..."

   가설 A-2: 배송 진행 알림 부재 [플랫폼/시스템]
   근거: voc_id=N "..."

   각 가설에 대해 채택(O)/수정(M)/기각(X)를 알려주세요.
   ```

3. **🛑 STOP — 사용자 응답 없이 11단계로 넘어가지 않는다.**

4. 채택/수정/기각 결과를 `output/review_log.jsonl`에 append 저장한다.

---

## 11단계 — 실행안·Deliverable 초안

**목적:** 채택된 원인 가설에 대해 Action Playbook 기반 실행안 초안을 생성한다.

### 지시

1. 채택된 각 원인 가설에 대해 `configs/commerce_cs.json`의 `playbook.actions`를 읽고
   대응하는 Action을 선택한다.
   - 목록에 없는 조치를 지어내지 않는다
   - 각 Action의 `cause` 코드와 채택 가설의 원인이 일치하는 것을 사용한다

2. 각 실행안에 포함할 내용:
   - **출처 Action** (코드 및 이름)
   - **구체적 실행 내용** (해당 데이터에서 관찰된 패턴 기반)
   - **Success Metric** (`configs/commerce_cs.json`의 `metrics.primary` 중 선택)
   - **직접 타깃** (대상 Topic의 건수·비율)
   - **기대효과** (Frequency 기반 정량 추정, 추정임을 명시)
   - **Deliverable 유형** (Jira 티켓 / Help Center 개편 / 판매자 가이드 / A/B 테스트 등)

3. 값이 내부 데이터가 필요한 경우 `[내부 확인 필수]`로 표시하고 비워둔다.
   추정값은 `[AI 추론 · 협의 후 확정]`으로 표시한다.

4. 결과를 `output/11_recommendation.md`로 저장한다.

---

## 12단계 — 검증·대시보드

**목적:** 분석 결과를 1페이지 요약으로 제시한다.

### 지시

1. `output/09_priority.csv`를 읽고 Priority 랭킹 표를 만든다.
2. 대분류별 VOC 비중, Sentiment 분포, Priority 등급을 요약한다.
3. "기타" 비율을 명시한다. 확장 카테고리가 있으면 그것도 포함한다.
4. 주요 실행안(11단계 산출)을 요약해서 함께 제시한다.
5. 한계 사항을 명시한다:
   - Trend 계산을 위해 합성 타임스탬프를 사용한 경우 반드시 고지:
     "created_at은 Trend 로직 검증을 위해 임의 부여한 값이며 실제 발생 시점이 아니다. Trend 수치는 실측이 아니다."
   - "기타" 비율이 높으면 택소노미 확장을 고려할 것을 권장

결과를 `output/12_dashboard.md`로 저장한다.

### 리포트 HTML 생성

`output/12_dashboard.md` 저장 후 아래 절차로 공유용 리포트를 생성한다.

1. `templates/REPORT_DATA_SCHEMA.md`를 읽고 스키마와 문체 규칙(개조식 필수)을 확인한다.

2. 아래 산출물을 종합해 `output/report_data.json`을 작성한다:
   - `output/06_classified.csv` — Topic 분류 분포
   - `output/07_sentiment.csv` — Sentiment 분포
   - `output/09_priority.csv` — F/S/T/Priority 수치
   - `output/review_log.jsonl` — Human 검토 기록 (validation 섹션)
   - `output/11_recommendation.md` — 실행안 (deliverables)
   - `configs/local_extensions.json` — 확장 카테고리 목록
   - **모든 텍스트는 SCHEMA의 "문체 규칙 — 개조식(필수)" 준수**
   - `status: "draft"` 확장 카테고리는 priority 배열에서 `status: "draft"`로 표기 (리포트가 '잠정' 배지를 자동 생성함)

3. `templates/report_template.html`을 읽는다.

4. 파일 내 문자열 `/*__REPORT_DATA__*/ null`을 `report_data.json`의 전체 내용으로 치환한다.
   - 결과는 `const DATA = { ... }` 형태로 HTML에 인라인 삽입됨
   - **`fetch()`로 별도 JSON을 읽게 만들지 않는다** — `file://`로 열 때 CORS 차단으로 빈 페이지가 됨

5. 치환 결과를 `output/report.html`로 저장한다.

6. 검증: `output/report.html`에 문자열 `const DATA = {`가 존재하는지 확인한다.
   존재하지 않으면 치환 실패 — 4번 절차를 재확인한다.

7. 사용자에게 경로 안내 및 경고를 전달한다:
   - 경로: `output/report.html` (브라우저로 직접 열람, 토픽별 PDF 개별 출력 가능)
   - **⚠️ 경고: 본 파일에 고객 발화 원문 포함. 외부 공유·커밋 전 내용 검토 요망.**

### 🛑 진행 차단 조건 — 리포트를 직접 작성하지 마라

**HTML을 새로 작성하는 것은 금지다.** 반드시 템플릿 치환 방식만 사용한다.

완료 보고 전에 `output/report.html`에서 아래 4개 문자열이 **모두** 존재하는지 확인하고
결과를 사용자에게 보고한다.

| 확인 문자열 | 의미 |
|---|---|
| `const DATA = {` | 데이터 치환 성공 |
| `sec-eyebrow` | 템플릿 유래 (섹션 헤더) |
| `dist-track` | 템플릿 유래 (분포 막대) |
| `print-single` | 템플릿 유래 (PDF 개별 출력) |

하나라도 없으면 **템플릿을 쓰지 않고 직접 작성한 것**이다. 그 경우 다음이 모두 사라진다:

- 확장 카테고리의 `잠정(draft)` 배지 — 근거 부족 카테고리가 확정된 것처럼 보인다
- **"Priority는 실행 순서가 아니다"** 경고 배너
- F/S/T 기여분 분해 막대 — 점수의 근거를 볼 수 없다
- 추세 산출 불가 경고
- 검증 절차 이행 내역 — 게이트를 통과했는지 확인 불가
- 산출물별 PDF 개별 출력

> **실제 재현 테스트 실패 사례:** 템플릿을 무시하고 HTML을 새로 작성한 실행이 있었다.
> 문서 자체는 보기 좋았으나 위 6가지가 전부 누락되었고, "실행 권고안(우선순위 순)"이라는
> 표현으로 **Priority를 실행 순서처럼 제시**해 SPEC §6.4를 정면으로 위반했다.

---

## 공통 규칙 (모든 단계에 적용)

- **결과 저장:** 각 단계 산출물은 반드시 `output/` 폴더에 아래 파일명으로 저장한다:
  - `01_cleaned.csv` — 정제 + 도메인 필터
  - `06_classified.csv` — Topic 분류
  - `07_sentiment.csv` — Sentiment 분류 (Topic 포함)
  - `09_priority.csv` — Priority 계산
  - `review_log.jsonl` — Human 검토 기록 (append)
  - `11_recommendation.md` — 실행안
  - `12_dashboard.md` — 대시보드

- **덮어쓰기:** 기존 파일이 있으면 사용자에게 알린다.

- **투명성:** 어떤 규칙으로 판단했는지 항상 사용자가 검증할 수 있게 근거를 함께 제시한다.

- **계산과 판단의 경계를 넘지 않는다:**
  - F/S/T/Priority → 스크립트 실행 결과만 인용. 암산 금지.
  - Topic/Sentiment/원인가설 → 당신의 판단으로 수행. 스크립트가 없다.

- **configs/commerce_cs.json 수정 금지:** 어떤 상황에서도 이 파일을 수정하지 않는다.
  사용자 확장은 `configs/local_extensions.json`에만 저장한다.
